# 게임 월드 병렬화 — 마 실험 동기화 기준서

> 본 문서는 `공통기준.md` 및 `병렬기준.md`를 기반으로, **마 실험(동기화 문제 유발 및 해결)**에 특화된 기준을 추가 정의한다.
> 계산 방식, 고정 상수, 검증값은 `공통기준.md`를 따른다.
> 병렬 출력 형식, Speedup/Efficiency 계산식은 `병렬기준.md`를 따른다.
> 이 문서는 동기화 수단 선택, 문제 유발 방식, 해결 구현 방식, 비교 출력 기준만 정의한다.

> **본 마 실험은 라 실험의 Child + Thread 혼합 구조를 대상으로 한다.**
> 동기화 문제는 Child 내부에서 생성된 worker thread들이 공유 height map(`tmap->data`)에 접근하는 과정에서 발생한다.
> Parent-Child 간 결과 전달은 기존 IPC 구조(pipe)를 유지하며, 본 문서는 **Child 내부 thread synchronization**을 중심으로 비교한다.

---

## 0. 문서 목적

마 실험의 목적은 두 단계다.

```text
[1단계] 동기화 문제 유발
  → BARRIER를 제거 또는 약화시켜 레이스 컨디션 재현
  → 무결성 실패 수 > 0 또는 결과값 불일치로 문제 증명

[2단계] 동기화 해결
  → 뮤텍스 / 세마포어 / 컨디션 배리어블 / 모니터 4가지 수단으로 각각 해결
  → BARRIER 1 / 2 / 3 중 실험 대상 배리어에 4가지 구현을 적용하여 성능 비교
  → Speedup, 동기화 오버헤드, Context Switching, 무결성 결과 비교 출력
```

---

## 1. 대상 코드 구조 요약

`final_parallel_experiment.c` 기준, `thread_worker()` 내 배리어 구조는 다음과 같다.

```text
[단계1] 노이즈 + 즉시병합         L545~557
   ↓
BARRIER 1   pthread_barrier_wait(bar)    L560
   ↓
[단계2] terrain_blur()  (tid==0 단독)   L569~575
   ↓
BARRIER 2   pthread_barrier_wait(bar)    L578
   ↓
[단계3] 침식 + 클램프               L580~624
   ↓
BARRIER 3   pthread_barrier_wait(bar)    L628
   ↓
verify_barrier   pthread_barrier_wait(verify_barrier)   L631
   ↓
play_barrier     pthread_barrier_wait(play_barrier)     L639
```

`BARRIER 1 / 2 / 3`은 동일한 `pthread_barrier_t *bar`를 재사용한다.
`verify_barrier`, `play_barrier`는 별도 배리어 객체다.

---

## 2. 실험 전략 — 전략 B 채택

BARRIER 1 / 2 / 3 각각에 대해 4가지 동기화 수단을 독립적으로 적용해 성능을 비교한다.

```text
비교 대상 (버전 공통):
  버전 A — pthread_mutex 기반 카운터 + while 스핀
  버전 B — sem_t 세마포어
  버전 C — pthread_cond_t 컨디션 배리어블
  버전 D — 모니터 구조 (mutex + cond 조합, 명시적 모니터 구현)

실험 단위:
  BARRIER 1 실험: BAR2/3 = pthread_barrier 유지, BAR1만 4가지 교체
  BARRIER 2 실험: BAR1/3 = pthread_barrier 유지, BAR2만 4가지 교체
  BARRIER 3 실험: BAR1/2 = pthread_barrier 유지, BAR3만 4가지 교체
```

배리어별 특성이 달라서 같은 4가지 수단이라도 자연스러운 적합성이 다르게 나온다.
이것이 실험의 핵심 분석 포인트다.

```text
BARRIER 1 → N개 스레드 전원 완료 대기       → 컨디션CV / 모니터가 자연스러움
BARRIER 2 → tid==0 단독 blur 완료 신호      → 세마포어 1→N 신호가 자연스러움
                                              → 뮤텍스 busy wait 영향이 가장 크게 드러남
                                                (blur 시간만큼 N-1개 스레드가 busy wait)
BARRIER 3 → 침식 완료 후 histogram 허용     → BARRIER 1과 동일한 N개 전원 완료 대기 구조
                                              → busy wait 영향은 침식 작업량 불균형에 따라 달라짐
```

---

## 3. 1단계 — 동기화 문제 유발

각 배리어를 독립적으로 제거해서 어떤 문제가 발생하는지 확인한다.
**한 번에 하나의 배리어만 제거**하고 나머지는 유지한다.

---

### 3.1 BARRIER 1 제거

`thread_worker()` L560의 `pthread_barrier_wait(bar)` 주석처리.

```c
// pthread_barrier_wait(bar);   /* BARRIER 1 제거 */

if (tid == 0) {
    terrain_blur(tmap);   /* 아직 noise 중인 tmap->data를 읽음 */
    ...
}
```

**예상 증상:**
```text
- terrain_blur()가 일부 chunk가 병합되지 않은 tmap->data를 읽음
- 0으로 초기화된 영역이 blur에 포함됨
- 무결성 실패 수 > 0 (경계 높이차 이상값 발생)
```

**유발 출력:**
```text
[동기화 문제 유발 — BARRIER 1 제거]
  제거 배리어: BARRIER 1 (noise+merge 완료 → blur 시작)
  유지 배리어: BARRIER 2, BARRIER 3
  무결성 실패: XXX 건  ← 0이 아니면 재현 성공
  Wall Time:  XXXXX.XX ms
  비고: blur가 불완전한 tmap 읽음 (일부 chunk 미병합)
```

---

### 3.2 BARRIER 2 제거

`thread_worker()` L578의 `pthread_barrier_wait(bar)` 주석처리.

```c
/* tid==0이 terrain_blur() 완료 */
if (tid == 0) {
    terrain_blur(tmap);
    a->elapsed_blur_ms = now_ms() - t;
}

// pthread_barrier_wait(bar);   /* BARRIER 2 제거 */

/* 침식 단계: blur가 완료되지 않은 tmap->data에 동시 write */
for (int v_id = 0; v_id < virtual_chunks; v_id++) { ... }
```

**예상 증상:**
```text
- tid==0이 blur 중인 tmap->data에 다른 스레드가 erosion write를 동시 수행
- blur write ↔ erosion write 충돌 → 데이터 오염
- 무결성 실패 수 > 0 (blur 결과가 침식에 의해 덮어써짐)
- 실행마다 결과가 달라지는 비결정적 동작 가능
```

**유발 출력:**
```text
[동기화 문제 유발 — BARRIER 2 제거]
  제거 배리어: BARRIER 2 (blur 완료 → erosion 시작)
  유지 배리어: BARRIER 1, BARRIER 3
  무결성 실패: XXX 건  ← 0이 아니면 재현 성공
  Wall Time:  XXXXX.XX ms
  비고: blur write ↔ erosion write 동시 접근으로 tmap 오염
```

---

### 3.3 BARRIER 3 제거 (보조 실험)

> **주의:** BARRIER 3 제거 실험은 재현이 안 될 가능성이 있다.
> 코드 구조상 BARRIER 3 직후에 `verify_barrier`가 존재하며,
> `verify_barrier`는 모든 worker thread + PARENT가 집결해야 통과하는 구조다.
> 따라서 BARRIER 3을 제거해도 `verify_barrier`가 사실상 동일한 역할을 대신할 수 있다.
> → histogram/integrity 조기 실행이 발생하지 않아 무결성 실패가 나오지 않을 수 있음.
> 이 실험은 선택 실험으로 진행하고, 결과가 나오지 않으면 그 이유를 분석 포인트로 서술한다.

`thread_worker()` L628의 `pthread_barrier_wait(bar)` 주석처리.

```c
/* 침식 + 클램프 완료 */
a->elapsed_erosion_ms = now_ms() - t;

// pthread_barrier_wait(bar);   /* BARRIER 3 제거 */

/* verify_barrier: 모든 thread + PARENT 집결 필요
   → verify_barrier가 BARRIER 3 역할을 사실상 대신할 수 있음 */
pthread_barrier_wait(a->verify_barrier);
```

**예상 증상:**
```text
경우 A (verify_barrier가 BARRIER 3 역할 대신):
  - 무결성 실패 = 0 그대로
  - 재현 실패 → "verify_barrier 구조로 인해 race condition 미발생" 분석으로 서술

경우 B (스레드 간 침식 완료 시간 차이가 클 때):
  - 일부 스레드가 verify_barrier 도달 전에 다른 스레드의 영역을 histogram이 읽음
  - 무결성 실패 > 0 또는 검증값 불일치 가능
```

**유발 출력:**
```text
[동기화 문제 유발 — BARRIER 3 제거 (보조 실험)]
  제거 배리어: BARRIER 3 (erosion+clamp 완료 → histogram/integrity 시작)
  유지 배리어: BARRIER 1, BARRIER 2
  verify_barrier 참가자: worker thread N개 + PARENT 1개 (g_num_threads+1명)
  무결성 실패: XXX 건  (0이면 verify_barrier가 대신함을 의미)
  Wall Time:  XXXXX.XX ms
  비고: verify_barrier 존재로 race condition 재현이 제한될 수 있음
```

---

### 3.4 주의사항

```text
- 한 번에 하나의 배리어만 제거한다.
- verify_barrier, play_barrier는 절대 건드리지 않는다. (크래시 위험)
- 문제 유발 버전은 별도 실행 파일로 빌드한다.
  gcc -O2 -o ma_race_b1 ma_race_b1.c -lm -lpthread
  gcc -O2 -o ma_race_b2 ma_race_b2.c -lm -lpthread
  gcc -O2 -o ma_race_b3 ma_race_b3.c -lm -lpthread
- BARRIER 제거 후에도 프로그램이 정상 종료되면 타이밍에 따라
  문제가 드러나지 않을 수 있다. 여러 번 반복 실행해서 확인한다.
- 문제 재현 성공 기준은 무결성 실패 > 0만이 아니다. 아래 중 하나라도 해당하면 재현 성공으로 간주한다.
  1. 무결성 실패 > 0
  2. 정상 실행(원본) 검증값과 불일치 (평균 조도, AI 최고점수, 파티클 에너지 중 하나라도)
  3. 동일 조건 반복 실행 시 결과값이 비결정적으로 변함 (실행마다 다른 값)
- 해결 버전 코드는 반드시 위 기준 중 하나로 문제 재현이 확인된 배리어 제거 버전 코드를 기준으로 작성한다.
  즉, 단순히 이론상 배리어만 제거한 코드가 아니라 무결성 실패/검증값 불일치/비결정성이 실제 확인된 코드에
  뮤텍스 / 세마포어 / 컨디션 배리어블 / 모니터를 각각 적용한다.
- 문제 재현을 위해 인위적 delay를 삽입했다면, 해결 버전 4개에도 동일한 delay 조건을 유지한다.
  그래야 동일한 문제 조건을 각 동기화 수단이 해결했는지 비교할 수 있다.
```

---

## 4. 2단계 — BARRIER 1 동기화 해결 4가지 구현

### 4.1 공통 전제

```text
- BARRIER 1만 교체한다. BARRIER 2, 3은 pthread_barrier_wait(bar) 유지.
- 각 버전은 동일한 작업(N개 스레드의 noise+merge 완료 대기)을 수행해야 한다.
- 모든 버전에서 무결성 실패 = 0이 되어야 한다.
- 전역 변수로 동기화 객체를 선언하고 main에서 초기화/소멸한다.
- 배리어 성격: N개 스레드 전원 완료 대기 → 컨디션CV / 모니터가 자연스러운 패턴

[재사용 주의]
- 각 동기화 객체는 1회 실행 기준으로 설계되어 있다.
- 같은 프로세스 안에서 반복 실험을 수행하는 경우,
  각 run 시작 전에 count, done, semaphore 값을 반드시 초기화한다.
- 배리어를 반복 재사용하려면 generation/sense-reversing barrier 구조가 필요하다.
```

---

### 4.2 버전 A — 뮤텍스 (Mutex)

#### 구조

```c
/* 전역 선언 — static initializer 사용 시 main에서 별도 init 불필요 */
static pthread_mutex_t  b1_mutex  = PTHREAD_MUTEX_INITIALIZER;
static int              b1_count  = 0;

/* thread_worker() 내 BARRIER 1 위치 */
pthread_mutex_lock(&b1_mutex);
b1_count++;
pthread_mutex_unlock(&b1_mutex);

/* 마지막 스레드가 아니면 완료될 때까지 스핀 대기 */
while (1) {
    pthread_mutex_lock(&b1_mutex);
    int done = (b1_count == g_num_threads);
    pthread_mutex_unlock(&b1_mutex);
    if (done) break;
    /* busy wait: sched_yield()로 CPU 양보 가능 */
}
```

#### 특성

```text
장점: 구현 단순, mutex 하나로 완전 제어
단점: busy wait로 CPU 낭비 발생 (nonvoluntary context switch 증가)
측정 포인트: nonvoluntary context switch 수 비교
```

#### 초기화 / 소멸 (main)

```c
/* static initializer(PTHREAD_MUTEX_INITIALIZER) 사용 시 pthread_mutex_init 불필요 */
/* 반복 실행 시 count만 초기화 */
pthread_mutex_lock(&b1_mutex);
b1_count = 0;
pthread_mutex_unlock(&b1_mutex);

/* main 종료 시 */
pthread_mutex_destroy(&b1_mutex);
```

---

### 4.3 버전 B — 세마포어 (Semaphore)

#### 구조

```c
#include <semaphore.h>

/* 전역 선언 — sem_t는 static initializer 없으므로 main에서 sem_init 필수 */
static sem_t b1_sem;
static pthread_mutex_t b1_cnt_mutex = PTHREAD_MUTEX_INITIALIZER;
static int b1_count = 0;

/* thread_worker() 내 BARRIER 1 위치 */
pthread_mutex_lock(&b1_cnt_mutex);
b1_count++;
int all_done = (b1_count == g_num_threads);
pthread_mutex_unlock(&b1_cnt_mutex);

if (all_done) {
    /* 마지막 스레드: N-1개 sem_post로 나머지 unblock */
    for (int i = 0; i < g_num_threads - 1; i++)
        sem_post(&b1_sem);
} else {
    /* 나머지 스레드: 대기 */
    sem_wait(&b1_sem);
}
```

#### 특성

```text
장점: 블로킹 대기 → CPU 낭비 없음
      1→N 신호 전달에 자연스러운 패턴
단점: N-1번 sem_post 필요, 재사용 시 초기화 주의
측정 포인트: voluntary context switch 수 비교 (블로킹이므로 증가)
```

#### 초기화 / 소멸 (main)

```c
sem_init(&b1_sem, 0, 0);

/* 실행 후 */
sem_destroy(&b1_sem);
pthread_mutex_destroy(&b1_cnt_mutex);
```

---

### 4.4 버전 C — 컨디션 배리어블 (Condition Variable)

#### 구조

```c
/* 전역 선언 — static initializer 사용 시 main에서 별도 init 불필요 */
static pthread_mutex_t b1_cv_mutex = PTHREAD_MUTEX_INITIALIZER;
static pthread_cond_t  b1_cond     = PTHREAD_COND_INITIALIZER;
static int             b1_count    = 0;

/* thread_worker() 내 BARRIER 1 위치 */
pthread_mutex_lock(&b1_cv_mutex);
b1_count++;
if (b1_count == g_num_threads) {
    /* 마지막 스레드: 전체 깨우기 */
    pthread_cond_broadcast(&b1_cond);
} else {
    /* 나머지 스레드: 조건 충족까지 대기 */
    while (b1_count < g_num_threads)
        pthread_cond_wait(&b1_cond, &b1_cv_mutex);
}
pthread_mutex_unlock(&b1_cv_mutex);
```

#### 특성

```text
장점: 교과서적 패턴, 불필요한 wake-up 없음
      블로킹 대기 → CPU 낭비 없음
      조건 변수로 "N개 완료" 의미를 명확히 표현
단점: spurious wakeup 방지를 위해 while 루프 필수
측정 포인트: voluntary context switch 수, 세마포어 버전과 비교
```

#### 초기화 / 소멸 (main)

```c
/* static initializer 사용 시 pthread_mutex_init / pthread_cond_init 불필요 */
/* 반복 실행 시 count만 초기화 */
pthread_mutex_lock(&b1_cv_mutex);
b1_count = 0;
pthread_mutex_unlock(&b1_cv_mutex);

/* main 종료 시 */
pthread_cond_destroy(&b1_cond);
pthread_mutex_destroy(&b1_cv_mutex);
```

---

### 4.5 버전 D — 모니터 (Monitor)

C에는 언어 차원의 모니터가 없으므로 `mutex + cond`로 모니터 구조를 직접 구현한다.
버전 C와 코드 구조는 유사하지만, **모니터의 enter/exit 개념을 명시적으로 구분**하여 보고서에 "모니터 구현"임을 서술한다.

#### 구조

```c
/* 모니터 구조체로 캡슐화 */
typedef struct {
    pthread_mutex_t lock;
    pthread_cond_t  cond;
    int             count;
    int             threshold;
} Monitor;

static Monitor b1_monitor;

/* 모니터 enter (임계 구역 진입) */
static void monitor_enter(Monitor *m) {
    pthread_mutex_lock(&m->lock);
}

/* 모니터 exit (임계 구역 탈출) */
static void monitor_exit(Monitor *m) {
    pthread_mutex_unlock(&m->lock);
}

/* 조건 대기 */
static void monitor_wait(Monitor *m) {
    while (m->count < m->threshold)
        pthread_cond_wait(&m->cond, &m->lock);
}

/* 조건 신호 */
static void monitor_notify_all(Monitor *m) {
    pthread_cond_broadcast(&m->cond);
}

/* thread_worker() 내 BARRIER 1 위치 */
monitor_enter(&b1_monitor);
b1_monitor.count++;
if (b1_monitor.count == b1_monitor.threshold) {
    monitor_notify_all(&b1_monitor);
} else {
    monitor_wait(&b1_monitor);
}
monitor_exit(&b1_monitor);
```

#### 특성

```text
장점: 모니터 개념(임계 구역 + 조건 변수)을 명시적으로 분리
      재사용 가능한 모니터 구조체
단점: 버전 C 대비 오버헤드 거의 동일하나 함수 호출 비용 미세 추가
측정 포인트: 버전 C와 Speedup/Context Switch 비교
보고서 명시: "이 구현이 모니터 패턴"임을 서술
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_init(&b1_monitor.lock, NULL);
pthread_cond_init(&b1_monitor.cond, NULL);
b1_monitor.count     = 0;
b1_monitor.threshold = g_num_threads;

/* 실행 후 */
pthread_cond_destroy(&b1_monitor.cond);
pthread_mutex_destroy(&b1_monitor.lock);
```

---

## 5. BARRIER 2 동기화 해결 4가지 구현

### 5.1 공통 전제

```text
- BARRIER 2만 교체한다. BARRIER 1, 3은 pthread_barrier_wait(bar) 유지.
- 배리어 성격: tid==0 단독 blur 완료 → 나머지 N-1개 스레드 unblock
  → 1개 신호원 → N-1개 수신자 구조 → 세마포어가 가장 자연스러운 패턴
- 모든 버전에서 무결성 실패 = 0이어야 한다.
- 전역 변수로 동기화 객체를 선언하고 main에서 초기화/소멸한다.
```

---

### 5.2 버전 A — 뮤텍스 (Mutex)

#### 구조

```c
/* 전역 선언 — static initializer 사용 시 main에서 별도 init 불필요 */
static pthread_mutex_t b2_mutex = PTHREAD_MUTEX_INITIALIZER;
static int b2_blur_done = 0;

/* thread_worker() 내 BARRIER 2 위치 */
if (tid == 0) {
    terrain_blur(tmap);
    a->elapsed_blur_ms = now_ms() - t;

    pthread_mutex_lock(&b2_mutex);
    b2_blur_done = 1;
    pthread_mutex_unlock(&b2_mutex);
} else {
    /* 나머지 스레드: blur 완료 플래그까지 busy wait */
    while (1) {
        pthread_mutex_lock(&b2_mutex);
        int done = b2_blur_done;
        pthread_mutex_unlock(&b2_mutex);
        if (done) break;
        /* sched_yield(); */
    }
}
```

#### 특성

```text
장점: 구현 단순
단점: N-1개 스레드가 모두 busy wait → CPU 낭비 심함
      blur가 오래 걸릴수록(약 수십 초) busy wait 시간 비례 증가
      BARRIER 2에서 뮤텍스는 적합성이 가장 낮음
측정 포인트: user CPU 시간이 다른 버전 대비 크게 증가하는지 확인
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_lock(&b2_mutex);
b2_blur_done = 0;
pthread_mutex_unlock(&b2_mutex);

pthread_mutex_destroy(&b2_mutex);
```

---

### 5.3 버전 B — 세마포어 (Semaphore)

#### 구조

```c
#include <semaphore.h>

/* 전역 선언 */
static sem_t b2_sem;

/* thread_worker() 내 BARRIER 2 위치 */
if (tid == 0) {
    terrain_blur(tmap);
    a->elapsed_blur_ms = now_ms() - t;

    /* N-1개 스레드를 한 번에 unblock */
    for (int i = 0; i < g_num_threads - 1; i++)
        sem_post(&b2_sem);
} else {
    sem_wait(&b2_sem);
}
```

#### 특성

```text
장점: 1→(N-1) 신호 전달 구조에 가장 자연스럽게 맞음
      블로킹 대기 → CPU 낭비 없음
      tid==0의 sem_post N-1번이 의미상 명확
단점: sem_post 반복 횟수가 스레드 수에 비례
측정 포인트: BARRIER 2에서 가장 낮은 동기화 오버헤드 예상
```

#### 초기화 / 소멸 (main)

```c
sem_init(&b2_sem, 0, 0);

sem_destroy(&b2_sem);
```

---

### 5.4 버전 C — 컨디션 배리어블 (Condition Variable)

#### 구조

```c
/* 전역 선언 */
static pthread_mutex_t b2_cv_mutex = PTHREAD_MUTEX_INITIALIZER;
static pthread_cond_t  b2_cond     = PTHREAD_COND_INITIALIZER;
static int             b2_blur_done = 0;

/* thread_worker() 내 BARRIER 2 위치 */
if (tid == 0) {
    terrain_blur(tmap);
    a->elapsed_blur_ms = now_ms() - t;

    pthread_mutex_lock(&b2_cv_mutex);
    b2_blur_done = 1;
    pthread_cond_broadcast(&b2_cond);
    pthread_mutex_unlock(&b2_cv_mutex);
} else {
    pthread_mutex_lock(&b2_cv_mutex);
    while (!b2_blur_done)
        pthread_cond_wait(&b2_cond, &b2_cv_mutex);
    pthread_mutex_unlock(&b2_cv_mutex);
}
```

#### 특성

```text
장점: blur 완료 조건을 명확히 표현
      블로킹 대기 → CPU 낭비 없음
      spurious wakeup 방지 while 루프 포함
단점: 세마포어 대비 코드량 많음
측정 포인트: 세마포어와 Speedup/Context Switch 비교
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_init(&b2_cv_mutex, NULL);
pthread_cond_init(&b2_cond, NULL);
b2_blur_done = 0;

pthread_cond_destroy(&b2_cond);
pthread_mutex_destroy(&b2_cv_mutex);
```

---

### 5.5 버전 D — 모니터 (Monitor)

#### 구조

```c
/* 기존 Monitor 구조체 재사용 (섹션 4.5 동일) */
static Monitor b2_monitor;

/* thread_worker() 내 BARRIER 2 위치 */
if (tid == 0) {
    terrain_blur(tmap);
    a->elapsed_blur_ms = now_ms() - t;

    monitor_enter(&b2_monitor);
    b2_monitor.count = b2_monitor.threshold;  /* 완료 플래그 */
    monitor_notify_all(&b2_monitor);
    monitor_exit(&b2_monitor);
} else {
    monitor_enter(&b2_monitor);
    /* count가 threshold(=완료 플래그)가 될 때까지 대기 */
    while (b2_monitor.count < b2_monitor.threshold)
        pthread_cond_wait(&b2_monitor.cond, &b2_monitor.lock);
    monitor_exit(&b2_monitor);
}
```

#### 특성

```text
장점: Monitor 구조체 재사용으로 코드 통일성 확보
단점: BARRIER 2는 1→N 신호 구조라 세마포어가 더 자연스럽고
      모니터는 상대적으로 억지스러운 매핑
      threshold 의미가 "blur 완료"가 아닌 "카운터 도달"로 왜곡될 수 있음
보고서 명시: "모니터 구조 적용 시 BARRIER 2의 신호 특성과 맞지 않아
             세마포어 대비 코드 가독성이 낮아짐"을 서술
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_init(&b2_monitor.lock, NULL);
pthread_cond_init(&b2_monitor.cond, NULL);
b2_monitor.count     = 0;
b2_monitor.threshold = 1;   /* tid==0이 1번 완료 신호 */

pthread_cond_destroy(&b2_monitor.cond);
pthread_mutex_destroy(&b2_monitor.lock);
```

---

## 6. BARRIER 3 동기화 해결 4가지 구현

### 6.1 공통 전제

```text
- BARRIER 3만 교체한다. BARRIER 1, 2는 pthread_barrier_wait(bar) 유지.
- 배리어 성격: N개 스레드의 침식+클램프 전원 완료 대기 후 PARENT histogram 허용
  → N개 전원 완료 대기이므로 BARRIER 1과 구조는 유사
  → 그러나 침식은 구역별 독립 write이므로 스레드 간 write 충돌은 없음
  → 완료 순서 차이가 클수록(느린 스레드 대기) busy wait 비용 차이가 크게 드러남
- 모든 버전에서 무결성 실패 = 0이어야 한다.
- 전역 변수로 동기화 객체를 선언하고 main에서 초기화/소멸한다.
```

---

### 6.2 버전 A — 뮤텍스 (Mutex)

#### 구조

```c
/* 전역 선언 */
static pthread_mutex_t b3_mutex = PTHREAD_MUTEX_INITIALIZER;
static int b3_count = 0;

/* thread_worker() 내 BARRIER 3 위치 (클램프 직후) */
pthread_mutex_lock(&b3_mutex);
b3_count++;
pthread_mutex_unlock(&b3_mutex);

while (1) {
    pthread_mutex_lock(&b3_mutex);
    int done = (b3_count == g_num_threads);
    pthread_mutex_unlock(&b3_mutex);
    if (done) break;
    /* sched_yield(); */
}
```

#### 특성

```text
장점: 구현 단순
단점: 침식은 스레드별 작업량이 고르므로 대기 시간이 짧을 수 있으나
      busy wait로 인해 CPU 활용률이 인위적으로 높아짐
측정 포인트: BARRIER 1 뮤텍스 버전과 nonvol context switch 비교
             침식 구역이 고르게 분배되므로 BARRIER 1보다 busy wait 시간이 짧을 수 있음
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_lock(&b3_mutex);
b3_count = 0;
pthread_mutex_unlock(&b3_mutex);

pthread_mutex_destroy(&b3_mutex);
```

---

### 6.3 버전 B — 세마포어 (Semaphore)

#### 구조

```c
#include <semaphore.h>

/* 전역 선언 */
static sem_t b3_sem;
static pthread_mutex_t b3_cnt_mutex = PTHREAD_MUTEX_INITIALIZER;
static int b3_count = 0;

/* thread_worker() 내 BARRIER 3 위치 */
pthread_mutex_lock(&b3_cnt_mutex);
b3_count++;
int all_done = (b3_count == g_num_threads);
pthread_mutex_unlock(&b3_cnt_mutex);

if (all_done) {
    for (int i = 0; i < g_num_threads - 1; i++)
        sem_post(&b3_sem);
} else {
    sem_wait(&b3_sem);
}
```

#### 특성

```text
장점: 블로킹 대기 → CPU 낭비 없음
단점: BARRIER 1과 구조 동일, 차별화 포인트는 침식 시간 분포에 따른 대기 시간
측정 포인트: BARRIER 1 세마포어 버전과 동기화 오버헤드 비교
```

#### 초기화 / 소멸 (main)

```c
sem_init(&b3_sem, 0, 0);
b3_count = 0;

sem_destroy(&b3_sem);
pthread_mutex_destroy(&b3_cnt_mutex);
```

---

### 6.4 버전 C — 컨디션 배리어블 (Condition Variable)

#### 구조

```c
/* 전역 선언 */
static pthread_mutex_t b3_cv_mutex = PTHREAD_MUTEX_INITIALIZER;
static pthread_cond_t  b3_cond     = PTHREAD_COND_INITIALIZER;
static int             b3_count    = 0;

/* thread_worker() 내 BARRIER 3 위치 */
pthread_mutex_lock(&b3_cv_mutex);
b3_count++;
if (b3_count == g_num_threads) {
    pthread_cond_broadcast(&b3_cond);
} else {
    while (b3_count < g_num_threads)
        pthread_cond_wait(&b3_cond, &b3_cv_mutex);
}
pthread_mutex_unlock(&b3_cv_mutex);
```

#### 특성

```text
장점: BARRIER 1과 동일 패턴 → 코드 일관성 확보
      침식 완료 조건("N개 전원 완료")을 명확히 표현
단점: 특별히 없음
측정 포인트: BARRIER 1 컨디션CV와 Speedup 비교
             두 배리어에서 같은 수단이 일관된 성능을 내는지 확인
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_init(&b3_cv_mutex, NULL);
pthread_cond_init(&b3_cond, NULL);
b3_count = 0;

pthread_cond_destroy(&b3_cond);
pthread_mutex_destroy(&b3_cv_mutex);
```

---

### 6.5 버전 D — 모니터 (Monitor)

#### 구조

```c
/* 기존 Monitor 구조체 재사용 */
static Monitor b3_monitor;

/* thread_worker() 내 BARRIER 3 위치 */
monitor_enter(&b3_monitor);
b3_monitor.count++;
if (b3_monitor.count == b3_monitor.threshold) {
    monitor_notify_all(&b3_monitor);
} else {
    monitor_wait(&b3_monitor);
}
monitor_exit(&b3_monitor);
```

#### 특성

```text
장점: BARRIER 1 모니터 버전과 완전히 동일한 구조 → Monitor 구조체 재사용
      침식 완료 조건에도 자연스럽게 맞음
단점: BARRIER 1 모니터와 구별되는 차별화 포인트가 적음
보고서 명시: "BARRIER 1과 BARRIER 3은 N개 전원 완료 대기라는 점에서 같은 구조이며
             모니터 패턴이 두 배리어 모두에서 일관되게 적용 가능함"을 서술
```

#### 초기화 / 소멸 (main)

```c
pthread_mutex_init(&b3_monitor.lock, NULL);
pthread_cond_init(&b3_monitor.cond, NULL);
b3_monitor.count     = 0;
b3_monitor.threshold = g_num_threads;

pthread_cond_destroy(&b3_monitor.cond);
pthread_mutex_destroy(&b3_monitor.lock);
```

---

## 7. 빌드 기준

```bash
# ── 문제 유발 버전 ──
gcc -O2 -o ma_race_b1   ma_race_b1.c   -lm -lpthread   # BARRIER 1 제거
gcc -O2 -o ma_race_b2   ma_race_b2.c   -lm -lpthread   # BARRIER 2 제거
gcc -O2 -o ma_race_b3   ma_race_b3.c   -lm -lpthread   # BARRIER 3 제거

# ── BARRIER 1 해결 버전 ──
gcc -O2 -o ma_b1_mutex      ma_b1_mutex.c      -lm -lpthread
gcc -O2 -o ma_b1_semaphore  ma_b1_semaphore.c  -lm -lpthread
gcc -O2 -o ma_b1_condvar    ma_b1_condvar.c    -lm -lpthread
gcc -O2 -o ma_b1_monitor    ma_b1_monitor.c    -lm -lpthread

# ── BARRIER 2 해결 버전 ──
gcc -O2 -o ma_b2_mutex      ma_b2_mutex.c      -lm -lpthread
gcc -O2 -o ma_b2_semaphore  ma_b2_semaphore.c  -lm -lpthread
gcc -O2 -o ma_b2_condvar    ma_b2_condvar.c    -lm -lpthread
gcc -O2 -o ma_b2_monitor    ma_b2_monitor.c    -lm -lpthread

# ── BARRIER 3 해결 버전 ──
gcc -O2 -o ma_b3_mutex      ma_b3_mutex.c      -lm -lpthread
gcc -O2 -o ma_b3_semaphore  ma_b3_semaphore.c  -lm -lpthread
gcc -O2 -o ma_b3_condvar    ma_b3_condvar.c    -lm -lpthread
gcc -O2 -o ma_b3_monitor    ma_b3_monitor.c    -lm -lpthread
```

---

## 8. 출력 형식

### 8.1 문제 유발 버전 출력 (배리어별 공통 형식)

```text
===========================================================
=== 게임 월드 병렬 실험 — 마 (동기화 문제 유발) ===
===========================================================
[동기화 설정]
  제거 배리어: BARRIER X  ← 1 / 2 / 3 중 해당 배리어 명시
  유지 배리어: BARRIER Y, BARRIER Z
  예상 문제:  (blur 입력 오염 / blur↔erosion 충돌 / histogram 조기 접근)

[세팅 단계]
  노이즈+병합:    XXXXX.XX ms
  블러:           XXXXX.XX ms
  침식:           XXXXX.XX ms
  세팅 Wall Time: XXXXX.XX ms

[결과 검증 — 문제 재현 확인]
  무결성 실패:    XXX 건  ← 0이 아니면 재현 성공
  평균 조도:      X.XXXXXX  (참고용)
===========================================================
```

### 8.2 해결 버전 비교 출력 (배리어별)

```text
===========================================================
=== 게임 월드 병렬 실험 — 마 (BARRIER X 동기화 해결 비교) ===
  스레드 수: N  |  베이스라인: XXXXX ms
===========================================================
[시스템 정보]
  시스템 코어 수:  XX개
  active core 수:  X개
  CPU Pinning:     적용 / 미적용

[BARRIER X — 동기화 수단별 비교]
  배리어 성격: (noise+merge 완료 대기 / blur 완료 신호 / erosion 완료 대기)
-----------------------------------------------------------
  버전        Wall Time    Speedup   vol.ctx   nonvol.ctx   sync오버헤드  무결성
  원본(bar)   XXXXX.XX ms  X.XXXx   XXXX 회   XXXX 회      X.XX ms      0
  뮤텍스      XXXXX.XX ms  X.XXXx   XXXX 회   XXXX 회      X.XX ms      0
  세마포어    XXXXX.XX ms  X.XXXx   XXXX 회   XXXX 회      X.XX ms      0
  컨디션CV    XXXXX.XX ms  X.XXXx   XXXX 회   XXXX 회      X.XX ms      0
  모니터      XXXXX.XX ms  X.XXXx   XXXX 회   XXXX 회      X.XX ms      0
-----------------------------------------------------------
[CPU 성능 지표]
  버전        user ms     sys ms    CPU 활용률
  원본(bar)   XXXXX.XX    XXX.XX    XXX.X%
  뮤텍스      XXXXX.XX    XXX.XX    XXX.X%  ← BARRIER 2에서 busy wait로 높을 것
  세마포어    XXXXX.XX    XXX.XX    XXX.X%
  컨디션CV    XXXXX.XX    XXX.XX    XXX.X%
  모니터      XXXXX.XX    XXX.XX    XXX.X%

[코어별 사용률]
  세팅 단계 코어별 사용률: core0=XX% core1=XX% ... (병렬기준.md 측정 방식 준용)
  플레이 단계 코어별 사용률: core0=XX% core1=XX% ...

[IPC 오버헤드]
  Parent-Child pipe IPC: XX.XX ms  (기존 구조 유지, 변경 없음)

[메모리 사용량]
  RSS:      XXXXX KB
  Peak RSS: XXXXX KB
-----------------------------------------------------------
[이 배리어에서 가장 적합한 수단 분석]
  적합: (세마포어 / 컨디션CV / 모니터)
  이유: (배리어 성격과 동기화 패턴의 매핑 이유 서술)
  비적합: 뮤텍스 (busy wait → user CPU 시간 X.XX ms 증가)
-----------------------------------------------------------
[Amdahl's Law 검증]
  버전        N  P_effective  이론 Speedup  실측 Speedup
  원본(bar)   X   XX.X%       X.XXXx       X.XXXx
  뮤텍스      X   XX.X%       X.XXXx       X.XXXx
  세마포어    X   XX.X%       X.XXXx       X.XXXx
  컨디션CV    X   XX.X%       X.XXXx       X.XXXx
  모니터      X   XX.X%       X.XXXx       X.XXXx
===========================================================
[결과 검증 — 모든 버전 동일해야 함]
  평균 조도:     0.397698  ✓
  AI 최고점수:   97.7063   ✓
  파티클 에너지: 25324.066406  ✓
  무결성 실패:   0         ✓
===========================================================
```

---

## 9. 측정 항목 및 방법

### 9.1 동기화 오버헤드 측정

각 배리어 진입 직전과 탈출 직후에 `now_ms()` 삽입. 스레드별 최대값을 기록한다.

```c
double t_sync_enter = now_ms();
/* 각 버전의 BARRIER 구현 */
double t_sync_exit  = now_ms();
double sync_overhead = t_sync_exit - t_sync_enter;
```

### 9.2 Context Switching

`getrusage(RUSAGE_SELF, &ru)` 사용. 세팅 단계 전/후 diff.

```c
struct rusage ru_before, ru_after;
getrusage(RUSAGE_SELF, &ru_before);
/* 세팅 단계 실행 */
getrusage(RUSAGE_SELF, &ru_after);

long vol    = ru_after.ru_nvcsw  - ru_before.ru_nvcsw;
long nonvol = ru_after.ru_nivcsw - ru_before.ru_nivcsw;
```

**Child+Thread 구조에서의 측정 주의사항:**
```text
- 마 실험 대상은 Child 내부 worker thread 동기화이므로
  getrusage(RUSAGE_SELF)는 반드시 Child process 내부에서 호출한다.
- RUSAGE_SELF는 해당 프로세스의 모든 thread를 합산하므로
  Child 내부 thread들의 context switch가 정확히 집계된다.
- Parent에서 전체 합산을 출력하려면 Child가 측정값을 IPC(pipe)로 전달하거나,
  Parent에서 waitpid() 이후 RUSAGE_CHILDREN로 별도 출력한다.
- 측정 한계가 있을 경우 보고서에 명시한다.
```

예상 패턴:
```text
뮤텍스(busy wait)          → nonvol 증가 두드러짐
세마포어 / 컨디션CV / 모니터 → vol 증가 (블로킹 대기)
BARRIER 2 뮤텍스            → blur 시간만큼 긴 busy wait → nonvol 가장 크게 증가
```

### 9.3 무결성 실패 수

`terrain_integrity()` 반환값 출력. 모든 해결 버전에서 0이어야 한다.

---

## 10. 배리어별 수단 적합성 비교 예상

```text
            BARRIER 1              BARRIER 2              BARRIER 3
            (N개 완료 대기)        (1→N 신호)             (N개 완료 대기)
뮤텍스      ⚠ busy wait           ✗ 가장 부적합          ⚠ busy wait
            noise 짧아서           blur 수십 초 동안       침식 작업량 불균형에
            영향 작을 수 있음       CPU 낭비 극심           따라 영향 달라짐
세마포어    ✓ 블로킹 대기          ✓ 가장 자연스러움       ✓ 블로킹 대기
컨디션CV    ✓✓ 교과서 패턴         ✓ 적합                  ✓✓ 교과서 패턴
모니터      ✓✓ 구조 명확           ⚠ 억지스러운 매핑       ✓✓ 구조 명확
```

보고서 서술 포인트:
```text
- BARRIER 1 / 3: 구조가 동일(N개 전원 완료 대기)하므로 컨디션CV/모니터가 두 배리어 모두에 일관되게 적합
- BARRIER 2: 1→N 신호 특성상 세마포어가 가장 자연스럽고, 뮤텍스는 가장 부적합
- 뮤텍스 busy wait 영향은 대기 시간에 비례하므로 blur가 긴 BARRIER 2에서 가장 크게 드러남
- BARRIER 3의 경우 verify_barrier 존재로 race condition 재현이 제한될 수 있으며,
  이 경우 "verify_barrier가 동기화를 대신하는 구조적 이유"를 분석 포인트로 서술
```

---

## 11. 적합성 판단 기준

각 배리어에서 4가지 동기화 수단 중 가장 적합한 것을 판단할 때 아래 6가지 기준을 순서대로 적용한다.

---

### 기준 1 — 무결성 실패 수

```text
판단 방법:
  terrain_integrity() 반환값 확인
  → 0이어야 해결 성공
  → 0이 아니면 동기화가 올바르게 동작하지 않은 것 → 해당 버전 탈락

적용:
  모든 버전에서 기준 1을 통과해야 이후 기준을 비교한다.
  기준 1을 통과하지 못한 버전은 적합성 판단 대상에서 제외한다.
```

---

### 기준 2 — 검증값 원본 일치

```text
판단 방법:
  아래 3가지 값이 원본(Baseline)과 동일한지 확인한다.
    평균 조도:     0.397698
    AI 최고점수:   97.7063
    파티클 에너지: 25324.066406

  → 3가지 모두 일치해야 정상
  → 하나라도 불일치하면 동기화 오류 또는 계산 순서 문제 의심

적용:
  기준 1 통과 후 기준 2도 통과해야 이후 기준 비교 가능.
  불일치 시 원인 분석 후 보고서에 명시한다.
```

---

### 기준 3 — Wall Time 및 Speedup

```text
판단 방법:
  세팅 Wall Time 및 실측 Speedup 비교
    실측 Speedup = Baseline Wall Time / 세팅 Wall Time

  판단 기준:
  - 원본(pthread_barrier) 대비 Wall Time 증가폭이 작을수록 적합
  - Speedup이 높을수록 적합
  - 3회 반복 평균값으로 비교 (1회 측정 결과만으로 판단 금지)

적용:
  버전 간 Wall Time 차이가 오차 범위(±2%) 이내면 성능 동등으로 간주.
  뮤텍스(busy-wait) 버전은 Wall Time이 다른 버전보다 길거나 비슷해도
  기준 5에서 CPU 낭비를 이유로 감점 요인이 된다.
```

---

### 기준 4 — BARRIER sync overhead

```text
판단 방법:
  BARRIER 진입 직전 ~ 탈출 직후 시간 (스레드별 최대값)
  → 값이 작을수록 동기화 수단 자체의 오버헤드가 작음

  비교 포인트:
  - 세마포어 / 컨디션CV / 모니터: 블로킹 대기이므로 sync overhead가 대기 시간에 해당
    → 작업량이 고를수록 sync overhead가 작아짐
  - 뮤텍스(busy-wait): sync overhead 자체는 짧게 나올 수 있으나
    그 시간 동안 CPU를 점유하므로 기준 5와 함께 평가해야 함

적용:
  sync overhead만 단독으로 보지 않는다.
  기준 3(Wall Time)과 기준 5(CPU 낭비)와 함께 종합 판단한다.
```

---

### 기준 5 — CPU 사용률 및 nonvoluntary context switch

```text
판단 방법:
  아래 두 항목을 확인한다.
    CPU user 시간 (ms)
    nonvoluntary context switch 수 (ru_nivcsw diff)

  판단 기준:
  - busy-wait(뮤텍스) 버전은 스핀 중 CPU를 점유하므로
    user 시간과 nonvol ctx sw 수가 다른 버전보다 높게 나올 것
  - 블로킹 버전(세마포어/컨디션CV/모니터)은
    voluntary ctx sw 수는 높지만 nonvol은 낮을 것

  기준 위반 조건:
  - 뮤텍스 버전에서 nonvol ctx sw가 다른 버전 대비 현저히 높거나
    CPU user 시간이 Wall Time보다 N배(코어 수)를 초과하여 높으면
    "비효율적 CPU 사용"으로 감점 요인으로 기록한다.

  BARRIER 2에서 뮤텍스는 blur 시간(수십 초)만큼 busy-wait하므로
  기준 5에서 가장 크게 감점된다.
```

---

### 기준 6 — 배리어 성격과 동기화 수단의 구조적 대응

```text
판단 방법:
  배리어가 요구하는 동기화 패턴과 수단의 구조가 자연스럽게 대응되는지 평가한다.

  배리어별 성격:
    BARRIER 1: N개 스레드 전원 완료 대기
      → 컨디션CV / 모니터: N개 완료 조건을 명확히 표현 → 자연스러움 ✓✓
      → 세마포어: N-1개 sem_post 필요 → 동작하지만 의미 표현이 어색 ✓
      → 뮤텍스: busy-wait → 동작하지만 비효율 ⚠

    BARRIER 2: tid==0 단독 완료 → N-1개 unblock (1→N 신호)
      → 세마포어: 1→N 신호 구조와 정확히 일치 → 가장 자연스러움 ✓✓
      → 컨디션CV: blur_done 조건 변수로 표현 → 적합 ✓
      → 모니터: threshold 의미가 왜곡됨 → 동작하지만 억지 ⚠
      → 뮤텍스: blur 동안 N-1개 busy-wait → 가장 부적합 ✗

    BARRIER 3: N개 스레드 전원 완료 대기 (BARRIER 1과 동일 구조)
      → 컨디션CV / 모니터: BARRIER 1과 동일하게 자연스러움 ✓✓
      → 세마포어: 블로킹 대기 가능 ✓
      → 뮤텍스: 침식 작업량 불균형에 따라 busy-wait 영향 달라짐 ⚠

  판단:
  - 구조적 대응이 자연스러울수록 코드 가독성, 유지보수성, 오류 가능성 감소
  - 동작은 하더라도 구조가 억지스러운 경우 보고서에 "구조적 부적합" 이유를 명시한다.
```

---

### 종합 판단 방법

```text
기준 적용 순서:
  1 → 2 → (3, 4, 5 종합) → 6

  기준 1, 2: 통과/탈락 (정량적 합격 기준)
  기준 3, 4, 5: 성능 비교 (낮을수록 유리, 종합 점수화)
  기준 6: 구조 적합성 (정성 평가, 동점일 때 최종 판단 기준)

배리어별 최종 예상 결론:
  BARRIER 1 → 컨디션CV 또는 모니터 (기준 3~6 모두 우수)
  BARRIER 2 → 세마포어 (기준 6 가장 자연스러움, 기준 5 뮤텍스 탈락)
  BARRIER 3 → 컨디션CV 또는 모니터 (BARRIER 1과 동일 구조)

보고서 서술:
  "기준 1~2를 통과한 버전들 중, 기준 3~5의 성능 지표와
   기준 6의 구조적 적합성을 종합하여 최적 수단을 선정하였다."
```

## 12. 실험 반복 실행 기준

각 버전 3회 반복 실행, 평균 Wall Time 사용. (`병렬기준.md` 섹션 9 동일 기준 적용)

**BARRIER 1 비교표:**
```text
버전        1회차  2회차  3회차  평균   Speedup  sync오버헤드  무결성
유발(B1제거) XX ms  XX ms  XX ms  XX ms  X.XXx   N/A          XXX건
원본(bar)    XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
뮤텍스       XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
세마포어     XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
컨디션CV     XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
모니터       XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
```

**BARRIER 2 비교표:**
```text
버전        1회차  2회차  3회차  평균   Speedup  sync오버헤드  무결성
유발(B2제거) XX ms  XX ms  XX ms  XX ms  X.XXx   N/A          XXX건
원본(bar)    XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
뮤텍스       XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0  ← user CPU 높을 것
세마포어     XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
컨디션CV     XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
모니터       XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
```

**BARRIER 3 비교표:**
```text
버전        1회차  2회차  3회차  평균   Speedup  sync오버헤드  무결성
유발(B3제거) XX ms  XX ms  XX ms  XX ms  X.XXx   N/A          XXX건
원본(bar)    XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
뮤텍스       XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
세마포어     XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
컨디션CV     XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
모니터       XX ms  XX ms  XX ms  XX ms  X.XXx   X.XX ms      0
```

---

## 13. 체크리스트

```text
── 문제 유발 ──
[ ] BARRIER 1 제거 버전 빌드 및 무결성 실패 > 0 확인
[ ] BARRIER 2 제거 버전 빌드 및 무결성 실패 > 0 확인
[ ] BARRIER 3 제거 버전 빌드 및 무결성 실패 > 0 확인

── BARRIER 1 해결 ──
[ ] 뮤텍스 버전 빌드 및 무결성 실패 = 0 확인
[ ] 세마포어 버전 빌드 및 무결성 실패 = 0 확인
[ ] 컨디션 배리어블 버전 빌드 및 무결성 실패 = 0 확인
[ ] 모니터 버전 빌드 및 무결성 실패 = 0 확인

── BARRIER 2 해결 ──
[ ] 뮤텍스 버전 빌드 및 무결성 실패 = 0 확인
[ ] 세마포어 버전 빌드 및 무결성 실패 = 0 확인 (가장 자연스러운 패턴)
[ ] 컨디션 배리어블 버전 빌드 및 무결성 실패 = 0 확인
[ ] 모니터 버전 빌드 및 무결성 실패 = 0 확인

── BARRIER 3 해결 ──
[ ] 뮤텍스 버전 빌드 및 무결성 실패 = 0 확인
[ ] 세마포어 버전 빌드 및 무결성 실패 = 0 확인
[ ] 컨디션 배리어블 버전 빌드 및 무결성 실패 = 0 확인
[ ] 모니터 버전 빌드 및 무결성 실패 = 0 확인

── 측정 및 분석 ──
[ ] 각 버전 3회 반복 실행
[ ] 배리어별 동기화 오버헤드(진입/탈출 시간) 측정
[ ] Context Switching voluntary/nonvoluntary 배리어별 비교
[ ] CPU 활용률 비교 (BARRIER 2 뮤텍스 busy wait 영향이 가장 클 것)
[ ] Speedup/Efficiency 배리어별 비교 표 작성
[ ] Amdahl's Law P_effective 비교
[ ] 배리어별 수단 적합성 분석 보고서 작성
[ ] 보고서에 "모니터 버전이 mutex+cond 조합으로 모니터 패턴 구현" 명시
[ ] 보고서에 "BARRIER 2는 1→N 신호 특성상 세마포어가 가장 자연스럽고
     뮤텍스 busy wait 영향이 가장 크게 드러난다" 서술
[ ] 팀원 이름 미출력
[ ] Baseline Wall Time 기준 명시
```

---

## 14. 참조

```text
공통기준.md  → 계산식, 고정 상수, 검증값
병렬기준.md  → Speedup/Efficiency/Amdahl 계산식, 코어별 사용률, Context Switching 측정
본 문서      → 마 실험 동기화 유발/해결 구현 기준
               BARRIER 1 / 2 / 3 각각에 4가지 수단 적용 및 비교
```
