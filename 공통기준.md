# 게임 월드 실험 공통 기준서 — Final Revised

> 반영 사항: `terrain\_integrity()` 허용 오차, Amdahl N/P 구분, 파티클 effect 파라미터/seed, Perlin hash 공식, Play RNG/물리/raymarching 계산식을 Baseline 기준으로 명시했다.

## 0\. 문서 목적

이 문서는 모든 병렬화 실험에서 공통으로 유지해야 하는 **Baseline 계산 방식, 고정 상수, 결과 검증 기준**을 정의한다.

병렬화 실험의 목적은 계산식을 바꾸는 것이 아니라, Baseline과 동일한 작업을 다른 실행 구조로 수행하여 Wall Time을 줄이는 것이다.

따라서 모든 병렬화 코드는 다음 원칙을 반드시 지켜야 한다.

* Seed, Map 크기, Chunk 크기, 반복 횟수, 계산식은 Baseline과 동일해야 한다.
* 병렬화를 위해 연산량을 줄이거나 반복 횟수를 줄이면 안 된다.
* 결과 검증값을 임의로 맞추는 보정 코드를 넣으면 안 된다.
* `terrain\_blur()`를 임의의 간이 블러로 대체하면 안 된다.
* 이 문서는 병렬 구조나 확장 출력 형식을 강제하는 문서가 아니다.
* 병렬 구조와 확장 출력 형식은 별도의 병렬 기준서(`game\_world\_parallel\_design\_standard.md`)를 따른다.

\---

## 1\. 고정 컴파일 기준

모든 실험 코드는 다음 형식으로 컴파일하는 것을 기본으로 한다.

```bash
gcc -O2 -o <실행파일명> <소스파일명>.c -lm -lpthread
```

Baseline 코드가 pthread를 직접 사용하지 않더라도, 팀 전체 실험 비교에서는 컴파일 옵션을 최대한 통일한다.

불필요한 옵션인 `-std=c11`, `-Wall`, `-Wextra`는 공통 기준에 포함하지 않는다.

\---

## 2\. 고정 상수

### 2.1 전체 실험 공통

```c
#define FIXED\_SEED      0xDEAD4096U

#define MAP\_WIDTH       1024
#define MAP\_HEIGHT      1024
#define CHUNK\_SIZE      64
#define CHUNKS\_PER\_ROW  (MAP\_WIDTH  / CHUNK\_SIZE)
#define CHUNKS\_PER\_COL  (MAP\_HEIGHT / CHUNK\_SIZE)
#define TOTAL\_CHUNKS    (CHUNKS\_PER\_ROW \* CHUNKS\_PER\_COL)  // 256
```

### 2.2 지형 생성 관련

```c
#define OCTAVE\_COUNT    6
#define MC\_SAMPLES      160
#define BLUR\_RADIUS     2

#define EROSION\_DROPS   ((long)MAP\_WIDTH \* MAP\_HEIGHT \* 16)
#define EROSION\_STEPS   256

#define MAX\_CLIFF\_DIFF  0.6f
#define HEIGHT\_MIN      0.0f
#define HEIGHT\_MAX      1.0f
#define HISTOGRAM\_BINS  256
```

### 2.3 조도 계산 관련

```c
#define LIGHT\_SAMPLES   200000000L
```

### 2.4 몬스터 AI 관련

```c
#define AI\_MONSTERS     20
#define AI\_WEIGHTS      16
#define AI\_ANNEAL\_STEPS 325
#define AI\_COMBAT\_SIM   500
#define AI\_COMBAT\_TICKS 300
```

### 2.5 파티클 관련

```c
#define PARTICLE\_TYPES  4
#define PARTICLE\_COUNT  400
#define PARTICLE\_STEPS  2250
```

### 2.6 플레이 단계 관련

```c
#define PLAY\_TICKS          500
#define PHYSICS\_OBJECTS     5000
#define AI\_SIGHT\_RAYS       128
#define RENDER\_PASSES       8
#define DRAWCALL\_COUNT      2048
#define RAYMARCH\_STEPS      6000
```

\---

## 3\. Baseline 기준 전체 실행 순서

Baseline 기준 전체 흐름은 다음과 같다.

```text
1. 지형 생성
   노이즈 → 병합 → 블러 → 침식 → 히스토그램 + 무결성 검사

2. 조도 계산

3. 몬스터 AI 계산

4. 파티클 시뮬레이션

5. 플레이 단계
   물리/AI → 명령서 생성 → GPU 렌더링
```

병렬화는 이 계산 순서를 완전히 바꾸는 것이 아니라, 서로 독립 가능한 작업을 동시에 실행하도록 재배치하는 것이다.

Baseline 코드의 지형 생성 출력에서 Barrier 표기는 다음 3개 기준점으로 사용한다.

```text
BARRIER POINT 1: terrain_merge() 완료 후 terrain_blur() 시작 가능
BARRIER POINT 2: terrain_blur() 완료 후 terrain_erosion() 시작 가능
BARRIER POINT 3: terrain_erosion() 완료 후 terrain_histogram()/terrain_integrity() 시작 가능
```

주의:

* noise 완료 후 merge 시작 가능 여부는 병렬 코드에서 반드시 보장해야 한다.
* 다만 `game_world_baseline_v4.c`의 실제 출력 기준에서는 noise 완료 지점을 별도 Barrier 번호로 세지 않는다.

\---

## 4\. 공통 유틸리티 계산 기준

### 4.1 `now\_ms()`

`clock\_gettime(CLOCK\_MONOTONIC, \&ts)`를 사용해 ms 단위 시간을 측정한다.

### 4.2 `lcg()`

난수 발생기는 다음 LCG 식을 사용한다.

```c
\*s = (\*s) \* 1664525u + 1013904223u;
```

### 4.3 `lcg\_f()`

LCG 결과의 하위 24비트를 사용해 `\[0, 1)` 범위의 float 난수를 만든다.

```c
(float)(lcg(s) \& 0xFFFFFF) / (float)0x1000000
```

### 4.4 `clampf()`

```c
v < lo ? lo : v > hi ? hi : v
```

\---

## 5\. 지형 생성 계산 기준

지형 생성은 Baseline 기준으로 다음 함수 흐름을 유지해야 한다.

```text
init\_noise\_params()
↓
noise\_compute\_chunk()
  ├─ monte\_carlo()
  │   └─ octave\_stack()
  │       └─ perlin\_octave()
  └─ chunk\_blur()
↓
terrain\_merge()
↓
terrain\_blur()
↓
terrain\_erosion()
↓
terrain\_histogram()
↓
terrain\_integrity()
```

\---

## 6\. `init\_noise\_params()` 기준

```c
p->seed = FIXED\_SEED;
p->persistence = 0.5f;
p->lacunarity = 2.0f;
```

`mountain\_weight`와 `roughness`는 고정 Seed에서 LCG로 계산된다.

```c
p->mountain\_weight = 0.3f + lcg\_f(\&rng) \* 0.4f;
p->roughness       = 0.4f + lcg\_f(\&rng) \* 0.4f;
```

octave별 frequency / amplitude 초기화 루프도 반드시 유지한다.

```c
float freq = 1.0f;
float amp  = 1.0f;
float asum = 0.0f;

for (int i = 0; i < OCTAVE\_COUNT; i++) {
    p->frequency\[i] = freq \* p->roughness;
    p->amplitude\[i] = amp;
    asum += amp;
    freq \*= p->lacunarity;
    amp  \*= p->persistence;
}

for (int i = 0; i < OCTAVE\_COUNT; i++)
    p->amplitude\[i] /= asum;
```

주의:

* `roughness`는 octave frequency 계산에 실제로 사용된다.
* `mountain\_weight`는 초기화되고 출력되지만, 현재 Baseline 지형 계산식에서는 실제 height 계산에 직접 사용되지 않는다.
* 따라서 병렬화 코드에서 `mountain\_weight`를 새롭게 height 계산에 반영하면 Baseline과 불일치한다.

\---

## 7\. 노이즈 계산 기준

### 7.1 `perlin\_octave()`

각 octave는 다음 흐름으로 계산한다.

```text
입력 좌표 x, y
↓
fx = x \* freq
fy = y \* freq
↓
격자 좌표 ix, iy와 보간 좌표 tx, ty 계산
↓
quintic smoothing 함수 ux, uy 계산
↓
seed 기반 g00, g10, g01, g11 계산
↓
선형 보간 후 amp를 곱해 반환
```

smoothing 식:

```c
ux = tx\*tx\*tx\*(tx\*(tx\*6 - 15) + 10);
uy = ty\*ty\*ty\*(ty\*(ty\*6 - 15) + 10);
```

격자 hash 값은 반드시 다음 상수와 식을 사용한다. `1619`, `31337`, `1e-5f`를 임의 변경하면 Baseline과 다른 지형이 생성된다.

```c
uint32\_t s00 = seed ^ (uint32\_t)(ix\*1619 + iy\*31337);
uint32\_t s10 = seed ^ (uint32\_t)((ix+1)\*1619 + iy\*31337);
uint32\_t s01 = seed ^ (uint32\_t)(ix\*1619 + (iy+1)\*31337);
uint32\_t s11 = seed ^ (uint32\_t)((ix+1)\*1619 + (iy+1)\*31337);
```

격자값 `g00`, `g10`, `g01`, `g11`은 각각 다음 방식으로 계산한다.

```c
g00 = sinf((float)s00 \* 1e-5f) \* cosf((float)(s00 >> 8) \* 1e-5f);
g10 = sinf((float)s10 \* 1e-5f) \* cosf((float)(s10 >> 8) \* 1e-5f);
g01 = sinf((float)s01 \* 1e-5f) \* cosf((float)(s01 >> 8) \* 1e-5f);
g11 = sinf((float)s11 \* 1e-5f) \* cosf((float)(s11 >> 8) \* 1e-5f);
```

최종 보간식은 다음을 유지한다.

```c
float v0 = g00 + ux \* (g10 - g00);
float v1 = g01 + ux \* (g11 - g01);
return amp \* (v0 + uy \* (v1 - v0));
```

### 7.2 `octave\_stack()`

6개 octave 결과를 누적한다.

```c
for (int oct = 0; oct < OCTAVE\_COUNT; oct++)
    h += perlin\_octave(px, py,
                       p->frequency\[oct],
                       p->amplitude\[oct],
                       p->seed ^ (uint32\_t)(oct \* 0xDEAD));
```

### 7.3 `monte\_carlo()`

각 픽셀에 대해 `MC\_SAMPLES = 160`번 jitter sampling을 수행한다.

난수 seed:

```c
uint32\_t rng = p->seed ^ (uint32\_t)(px\*7919 + py\*6271);
```

jitter 좌표:

```c
jx = px + (lcg\_f(\&rng) - 0.5f) \* 0.1f;
jy = py + (lcg\_f(\&rng) - 0.5f) \* 0.1f;
```

가중치:

```c
w = expf(-(dx\*dx + dy\*dy) \* 8);
```

결과:

```c
sum / wsum
```

### 7.4 `noise\_compute\_chunk()`

각 chunk 내부의 64×64 픽셀에 대해 Monte Carlo height를 계산한다.

```c
h = monte\_carlo(px, py, p);
c->height\_data\[ly\*CHUNK\_SIZE + lx] = clampf((h+1)\*0.5f, HEIGHT\_MIN, HEIGHT\_MAX);
```

이후 반드시 `chunk\_blur()`를 수행한다.

\---

## 8\. 블러 계산 기준

블러는 두 단계가 있다.

```text
1단계: chunk\_blur()
   - 청크 내부 64×64 데이터에 대한 5×5 커널 블러

2단계: terrain\_blur()
   - 전체 map 병합 후 청크 경계면 보정 블러
```

두 블러는 역할이 다르며, 하나를 다른 하나로 대체하면 안 된다.

\---

### 8.1 `chunk\_blur()` 기준

`chunk\_blur()`는 각 chunk 내부 `height\_data`에 대해 5×5 커널을 적용한다.

커널은 다음 값을 사용한다.

```c
static const float K\[5]\[5] = {
    {0.00390625f,0.015625f,0.0234375f,0.015625f,0.00390625f},
    {0.015625f,  0.0625f,  0.09375f,  0.0625f,  0.015625f  },
    {0.0234375f, 0.09375f, 0.140625f, 0.09375f, 0.0234375f },
    {0.015625f,  0.0625f,  0.09375f,  0.0625f,  0.015625f  },
    {0.00390625f,0.015625f,0.0234375f,0.015625f,0.00390625f},
};
```

주의:

* `chunk\_blur()`는 chunk 내부만 본다.
* 인접 chunk의 값을 참조하지 않는다.
* 따라서 chunk 경계면 보정은 `terrain\_blur()`가 담당해야 한다.

\---

### 8.2 `terrain\_merge()` 기준

모든 chunk의 `height\_data`를 전체 `tmap->data`에 복사한다.

```text
chunk\_id 0\~255
↓
각 chunk의 64×64 height\_data를
전체 1024×1024 map의 대응 위치에 memcpy
```

병렬화 시에도 전체 chunk 병합이 완료된 뒤 `terrain\_blur()`가 시작되어야 한다.

\---

### 8.3 `terrain\_blur()` 기준

`terrain\_blur()`는 전체 map 기준으로 청크 경계면을 부드럽게 만드는 단계다.

고정 반복 횟수:

```c
const int ITER = 400;
```

처리 순서:

```text
for iter = 0 \~ 399:
    1. 전체 map을 tmp에 복사
    2. 가로 chunk 경계 row 주변을 blur
    3. 다시 전체 map을 tmp에 복사
    4. 세로 chunk 경계 column 주변을 blur
```

가로 경계 처리:

```text
cy = 1 \~ CHUNKS\_PER\_COL-1
경계 row gy = cy \* CHUNK\_SIZE
brow = gy - BLUR\_RADIUS \~ gy + BLUR\_RADIUS
```

세로 경계 처리:

```text
cx = 1 \~ CHUNKS\_PER\_ROW-1
경계 column gx = cx \* CHUNK\_SIZE
bcol = gx - BLUR\_RADIUS \~ gx + BLUR\_RADIUS
```

가중치 계산:

```c
var = (lss/3.0f) - (ls/3.0f) \* (ls/3.0f);
w = 1.0f / (1.0f + expf(-var \* 10.0f));
```

출력값 계산:

```c
acc += tmp\[neighbor] \* (1-w) + tmp\[current\_boundary] \* w;
map\[...] = acc / cnt;
```

중요:

* 병렬화 코드에서 `terrain\_blur()`를 단순 row blur로 대체하면 안 된다.
* Baseline과 동일 결과를 원하면 `terrain\_blur()`의 계산 의미를 유지해야 한다.
* 병렬화 시 읽기용 snapshot과 쓰기용 map이 섞이면 결과가 흔들릴 수 있다.
* 안전한 병렬화는 src/dst 버퍼 또는 명확한 barrier를 사용해야 한다.

\---

## 9\. 침식 계산 기준

`terrain\_erosion()`은 전체 height map을 기준으로 수력 침식을 수행한다.

고정값:

```c
const float er = 0.004f;
const float dr = 0.002f;
uint32\_t rng = tmap->params.seed ^ 0xBAADF00D;
```

각 drop의 시작 좌표는 단순한 `random % range`가 아니라, LCG를 직접 갱신하고 하위 24비트를 마스킹해서 계산한다.

```c
int fx = (int)(((rng = rng\*1664525u + 1013904223u) \& 0xFFFFFF) % (MAP\_WIDTH-2)) + 1;
int fy = (int)(((rng = rng\*1664525u + 1013904223u) \& 0xFFFFFF) % (MAP\_HEIGHT-2)) + 1;
```

각 drop은 최대 `EROSION\_STEPS = 256`번 이동한다.

주변 4방향 중 가장 낮은 위치로 이동한다.

```text
right, left, down, up 중 height가 가장 낮은 방향 선택
```

침식 및 퇴적:

```c
diff = hc - hm;
map\[fy]\[fx] -= er \* diff;
map\[fy+dy]\[fx+dx] += dr \* diff;
```

마지막에 전체 map을 `\[HEIGHT\_MIN, HEIGHT\_MAX]`로 clamp한다.

병렬화 주의:

* 침식은 map을 직접 수정하는 작업이다.
* 여러 worker가 같은 위치나 경계 위치를 동시에 수정하면 Baseline과 달라질 수 있다.
* 병렬화 시 영역 분할, 경계 처리, RNG 순서가 Baseline 결과와 달라질 수 있음을 명확히 기록해야 한다.

\---

## 10\. 히스토그램 평활화 기준

`terrain\_histogram()`은 전체 map에 대해 256개 bin을 사용한다.

```c
int b = (int)(map\[y]\[x] \* (HISTOGRAM\_BINS - 1));
```

CDF 계산 후 다음 식으로 정규화한다.

```c
cdf\[b] = (cdf\[b] - (float)cmin) / (float)(tot - cmin);
cdf\[b] = clampf(cdf\[b], 0, 1);
```

마지막으로 각 height를 CDF 값으로 치환한다.

\---

## 11\. 무결성 검사 기준

`terrain\_integrity()`는 다음을 검사한다.

1. height 값이 허용 범위 안에 있는지 검사
2. 오른쪽 이웃과의 차이가 `MAX\_CLIFF\_DIFF`를 넘는지 검사
3. 아래쪽 이웃과의 차이가 `MAX\_CLIFF\_DIFF`를 넘는지 검사

중요: height 범위 검사는 엄격한 `\[0.0f, 1.0f]`가 아니다. Baseline 코드는 부동소수점 오차를 고려해 ±`1e-4f` 허용 오차를 둔다.

```c
if (h < HEIGHT\_MIN - 1e-4f || h > HEIGHT\_MAX + 1e-4f)
    fail++;
```

경사 차이 검사:

```c
if (x+1 < MAP\_WIDTH  \&\& fabsf(h - tmap->data\[y]\[x+1]) > MAX\_CLIFF\_DIFF) fail++;
if (y+1 < MAP\_HEIGHT \&\& fabsf(h - tmap->data\[y+1]\[x]) > MAX\_CLIFF\_DIFF) fail++;
```

최종 기준:

```text
무결성 실패: 0
```

\---

## 12\. Parent 조도 계산 기준

`parent\_lighting\_simulation()`은 Monte Carlo 방식으로 평균 조도를 계산한다.

고정 seed:

```c
uint32\_t rng = FIXED\_SEED ^ 0xABCD1234;
```

샘플 수:

```c
LIGHT\_SAMPLES = 200000000L
```

각 sample에서 hemisphere 방향을 생성하고, sun vector와의 direct term 및 scatter term을 더한다.

태양 방향 초기화:

```c
float sun\_az  = lcg\_f(\&rng) \* 2.0f \* 3.14159f;
float sun\_alt = 0.2f + lcg\_f(\&rng) \* 0.6f;
float sun\_x   = cosf(sun\_alt) \* cosf(sun\_az);
float sun\_y   = sinf(sun\_alt);
float sun\_z   = cosf(sun\_alt) \* sinf(sun\_az);
```

hemisphere 샘플 방향:

```c
float theta = acosf(sqrtf(u));
float phi   = 2.0f \* 3.14159f \* v;
float rx    = sinf(theta) \* cosf(phi);
float ry    = cosf(theta);
float rz    = sinf(theta) \* sinf(phi);
```

```c
direct = rx\*sun\_x + ry\*sun\_y + rz\*sun\_z;
if (direct < 0) direct = 0;
scatter = expf(-sx\*sx\*2.0f - sy\*sy\*2.0f) \* 0.3f;
total += direct + scatter;
```

결과:

```c
result = total / LIGHT\_SAMPLES;
```

검증 기준:

```text
평균 조도: 0.397698 근처
```

\---

## 13\. Child1 몬스터 AI 계산 기준

`child1\_monster\_ai()`는 20종 몬스터에 대해 시뮬레이티드 어닐링 방식으로 행동 가중치를 최적화한다.

고정 seed:

```c
uint32\_t rng = FIXED\_SEED ^ 0xDEADBEEF;
```

고정 반복:

```text
AI\_MONSTERS = 20
AI\_ANNEAL\_STEPS = 325
AI\_COMBAT\_SIM = 500
AI\_COMBAT\_TICKS = 300
```

cooling 공식:

```c
const float cool = 1.0f - (1.0f - 1e-4f) / AI\_ANNEAL\_STEPS;
T \*= cool;
```

weight 초기화 및 후보 생성:

```c
w\[i] = best\_w\[i] = lcg\_f(\&rng) \* 2 - 1;
cand\[i] = w\[i] + (lcg\_f(\&rng) - 0.5f) \* T \* 0.5f;
```

전투 파라미터 계산:

```c
matk = fabsf(cand\[0]) \* 10 + 5;
aggr = clampf(cand\[1] \* 0.5f + 0.5f, 0, 1);
evad = clampf(cand\[2] \* 0.5f + 0.5f, 0, 1);
```

행동 판단:

```c
hr    = mhp / 100.0f;
atk\_s = cand\[3] \* hr    + cand\[4] \* aggr;
ret\_s = cand\[5] \* (1-hr) + cand\[6] \* evad;
```

공격 선택 시 데미지 및 HP 갱신:

```c
dmg = matk \* (1 + cand\[7] \* 0.2f);
for (int k = 0; k < AI\_WEIGHTS; k++)
    dmg += cand\[k] \* cand\[k] \* 0.01f;

php -= dmg \* 0.016f;
mhp -= 8 \* (1 - evad \* 0.3f) \* 0.016f;
```

후퇴 선택 시:

```c
ev = evad \* (1 + sinf((float)tick \* 0.1f) \* 0.2f);
mhp -= 8 \* (1 - ev) \* 0.016f;
```

annealing 수용 조건:

```c
delta = score - best\_score;
if (delta > 0 || lcg\_f(\&rng) < expf(delta / T)) { /\* accept \*/ }
```

전투 score 계산식:

```c
score\_sum += (100 - php) \* 0.6f + mhp \* 0.4f;
score = score\_sum / AI\_COMBAT\_SIM;
```

최종 검증 기준:

```text
AI 최고점수: 97.7063 근처
```

\---

## 14\. Child2 파티클 계산 기준

`child2\_particle\_simulation()`은 4개 effect를 순차 계산한 후 평균 에너지를 반환한다.

파티클 종류는 다음 4개이며, 각 effect의 물리 파라미터는 Baseline 코드와 동일해야 한다.

|Type|gravity|viscosity|init\_speed|eps|sigma|lt\_max|
|-|-:|-:|-:|-:|-:|-:|
|EXPLOSION|9.8f|0.01f|15|0.5f|0.3f|2|
|SPLASH|9.8f|0.1f|5|1.0f|0.5f|4|
|LAVA|9.8f|2.0f|2|2.0f|0.8f|8|
|SMOKE|-1.0f|0.05f|1|0.1f|1.0f|12|

각 effect는 `PARTICLE\_COUNT = 400`, `PARTICLE\_STEPS = 2250` 기준으로 시뮬레이션한다.

파티클 seed XOR 방식은 결과 재현에 직접 영향을 주므로 반드시 유지한다.

```c
// child2\_particle\_simulation()에서 각 effect 호출 시
for (int t = 0; t < PARTICLE\_TYPES; t++)
    total += simulate\_effect((EffectType)t, seed ^ (uint32\_t)(t \* 0x5678));

// simulate\_effect() 내부
uint32\_t rng = seed ^ (uint32\_t)(type \* 0x1234);
```

초기 파티클 상태 생성식도 유지한다.

```c
p\[i].x = (lcg\_f(\&rng) - 0.5f) \* 2;
p\[i].y = (lcg\_f(\&rng) - 0.5f) \* 2;
p\[i].z = (lcg\_f(\&rng) - 0.5f) \* 2;

float spd = init\_speed \* (0.5f + lcg\_f(\&rng) \* 0.5f);
p\[i].vx = (lcg\_f(\&rng) - 0.5f) \* spd;
p\[i].vy = (lcg\_f(\&rng) - 0.5f) \* spd;
p\[i].vz = (lcg\_f(\&rng) - 0.5f) \* spd;

p\[i].mass     = 0.5f + lcg\_f(\&rng) \* 0.5f;
p\[i].charge   = lcg\_f(\&rng) \* 2 - 1;
p\[i].lifetime = lt\_max \* (0.5f + lcg\_f(\&rng) \* 0.5f);
```

파티클 상호작용은 Lennard-Jones 형태의 힘을 사용한다.

lifetime이 0 이하인 파티클은 건너뛴다.

```c
if (p\[i].lifetime <= 0) continue;
if (p\[j].lifetime <= 0) continue;
```

거리 최솟값 조건으로 수치 발산을 방지한다.

```c
float r2 = dx\*dx + dy\*dy + dz\*dz;
if (r2 < 0.01f) continue;
float r = sqrtf(r2);
```

LJ 힘 계산:

```c
float sr = sigma / r;
float sr6 = sr\*sr\*sr\*sr\*sr\*sr;
float sr12 = sr6 \* sr6;
float fmag = 24 \* eps \* (2\*sr12 - sr6) / r2;
```

힘 방향 적용 (작용·반작용):

```c
p\[i].fx -= fmag \* dx;  p\[i].fy -= fmag \* dy;  p\[i].fz -= fmag \* dz;
p\[j].fx += fmag \* dx;  p\[j].fy += fmag \* dy;  p\[j].fz += fmag \* dz;
```

중력 및 점성 항:

```c
p\[i].fy += gravity \* p\[i].mass;
p\[i].fx -= viscosity \* p\[i].vx;
p\[i].fy -= viscosity \* p\[i].vy;
p\[i].fz -= viscosity \* p\[i].vz;
```

적분 time step:

```c
const float DT = 0.001f;
```

힘은 `\[-1e5f, 1e5f]` 범위로 제한한다.

```c
if (fmag >  1e5f) fmag =  1e5f;
if (fmag < -1e5f) fmag = -1e5f;
```

속도는 `\[-1e4f, 1e4f]` 범위로 제한한다.

```c
if (p\[i].vx >  1e4f) p\[i].vx =  1e4f;
if (p\[i].vx < -1e4f) p\[i].vx = -1e4f;
if (p\[i].vy >  1e4f) p\[i].vy =  1e4f;
if (p\[i].vy < -1e4f) p\[i].vy = -1e4f;
if (p\[i].vz >  1e4f) p\[i].vz =  1e4f;
if (p\[i].vz < -1e4f) p\[i].vz = -1e4f;
```

SMOKE effect만 추가적인 난류 항을 가진다.

```c
if (type == SMOKE) {
    p\[i].fx += 0.5f \* sinf(p\[i].y \* 3 + (float)step \* DT \* 2);
    p\[i].fz += 0.5f \* cosf(p\[i].x \* 3 + (float)step \* DT \* 2);
}
```

에너지 누적 및 반환:

```c
total\_ke += 0.5 \* p\[i].mass \* (p\[i].vx\*p\[i].vx + p\[i].vy\*p\[i].vy + p\[i].vz\*p\[i].vz);

result = (float)(total\_ke / (PARTICLE\_STEPS \* PARTICLE\_COUNT));
```

결과는 4개 effect 평균이다.

검증 기준:

```text
파티클 에너지: 25324.066406 근처
```

\---

## 15\. 플레이 단계 계산 기준

Baseline 플레이 단계는 순차 파이프라인이다.

```text
tick\_physics\_ai()
↓
tick\_build\_commands()
↓
tick\_render()
```

플레이 단계 전체 RNG seed는 다음 값으로 고정한다.

```c
uint32\_t rng = FIXED\_SEED ^ 0xCAFEF00D;
```

병렬화 시에도 tick 순서, RNG 소비 순서, DrawCall 생성 순서가 바뀌면 결과가 달라질 수 있으므로 주의한다.

### 15.1 `tick\_physics\_ai()`

각 tick마다 `PHYSICS\_OBJECTS = 5000`개 object를 처리하고, object마다 `AI\_SIGHT\_RAYS = 128`개 ray를 검사한다.

object 초기 위치와 속도 생성식은 다음을 유지한다.

```c
float px = lcg\_f(rng) \* MAP\_WIDTH;
float py = lcg\_f(rng) \* MAP\_HEIGHT;
float vx = (lcg\_f(rng) - 0.5f) \* 2.0f;
float vy = (lcg\_f(rng) - 0.5f) \* 2.0f;
```

지형 높이 참조 및 물리 업데이트 식은 다음과 같다.

```c
float h = tmap->data\[(int)clampf(py, 0, MAP\_HEIGHT-1)]
                    \[(int)clampf(px, 0, MAP\_WIDTH-1)];

vy += (9.8f \* h - 0.1f \* vy) \* 0.016f;
vx += (-0.1f \* vx) \* 0.016f;

px += vx \* 0.016f;
py += vy \* 0.016f;
px = clampf(px, 0, MAP\_WIDTH-1);
py = clampf(py, 0, MAP\_HEIGHT-1);
```

AI 시야 ray 생성식은 다음과 같다.

```c
float angle = lcg\_f(rng) \* 6.2832f;
float dist  = 1.0f + lcg\_f(rng) \* 64.0f;
float tx    = px + cosf(angle) \* dist;
float ty    = py + sinf(angle) \* dist;
```

ray hit 판정 기준은 다음과 같다.

```c
if (th > h + 0.1f) hits++;
```

### 15.2 `tick\_build\_commands()`

`DRAWCALL\_COUNT = 2048`개의 DrawCall을 생성한다.

DrawCall 위치 생성식은 다음과 같다.

```c
float px = lcg\_f(rng) \* (MAP\_WIDTH  - 2);
float py = lcg\_f(rng) \* (MAP\_HEIGHT - 2);
int ix = (int)px;
int iy = (int)py;
```

normal 계산식은 다음과 같다.

```c
float dh\_dx = tmap->data\[iy]\[ix+1] - tmap->data\[iy]\[ix];
float dh\_dy = tmap->data\[iy+1]\[ix] - tmap->data\[iy]\[ix];
float len = sqrtf(dh\_dx\*dh\_dx + dh\_dy\*dh\_dy + 1.0f);

cmds\[i].normal\_x = -dh\_dx / len;
cmds\[i].normal\_y =  1.0f  / len;
cmds\[i].normal\_z = -dh\_dy / len;
```

light intensity와 pass mask 계산식은 다음과 같다.

```c
cmds\[i].light\_intensity = clampf(
    (float)hits / (float)(PHYSICS\_OBJECTS \* AI\_SIGHT\_RAYS),
    0.0f, 1.0f);

cmds\[i].pass\_mask = 1 << (i % RENDER\_PASSES);
```

### 15.3 `tick\_render()`

각 DrawCall에 대해 `RENDER\_PASSES = 8` pass를 돌고, raymarching은 `RAYMARCH\_STEPS = 6000` 기준으로 수행한다.

ray origin은 다음 값으로 고정한다.

```c
float ray\_ox = 512.0f;
float ray\_oy = 2.0f;
float ray\_oz = 512.0f;
```

ray direction 계산식은 다음과 같다.

```c
float ray\_dx = cmds\[i].pos\_x - ray\_ox;
float ray\_dy = cmds\[i].pos\_y - ray\_oy;
float ray\_dz = cmds\[i].pos\_z - ray\_oz;
float ray\_len = sqrtf(ray\_dx\*ray\_dx + ray\_dy\*ray\_dy + ray\_dz\*ray\_dz);
ray\_dx /= ray\_len;
ray\_dy /= ray\_len;
ray\_dz /= ray\_len;
```

occlusion 계산은 `sy < terrain\_h` 조건을 기준으로 누적한다.

```c
float occlusion = 0.0f;
for (int s = 0; s < RAYMARCH\_STEPS; s++) {
    float t = ray\_len \* (float)s / RAYMARCH\_STEPS;
    float sx = ray\_ox + ray\_dx \* t;
    float sy = ray\_oy + ray\_dy \* t;
    float sz = ray\_oz + ray\_dz \* t;

    int mx = (int)clampf(sx, 0, MAP\_WIDTH-1);
    int mz = (int)clampf(sz, 0, MAP\_HEIGHT-1);
    float terrain\_h = tmap->data\[mz]\[mx];

    if (sy < terrain\_h) occlusion += 1.0f;
}
occlusion /= RAYMARCH\_STEPS;
```

pixel 계산식은 다음을 유지한다.

```c
float pixel = (1.0f - occlusion)
            \* cmds\[i].light\_intensity
            \* (1.0f + sinf((float)tick \* 0.1f
                         + cmds\[i].pos\_x \* 0.01f) \* 0.05f);
```

병렬화 시에도 물리/AI, 명령서 생성, 렌더링의 계산식 자체는 바꾸면 안 된다.

\---

## 16\. 결과 검증 기준

모든 병렬화 실험은 Baseline 결과와 동일하거나 오차 허용 범위 내에 있어야 한다.

```text
평균 조도:     0.397698  (소수점 6자리 기준 동일, 허용 오차 ±1e-6)
AI 최고점수:   97.7063   (소수점 4자리 기준 동일)
파티클 에너지: 25324.066406  (소수점 6자리 기준 동일, 허용 오차 매우 작은 float 오차만)
무결성 실패:   0         (반드시 0)
```

주의:

* CPU / 컴파일러 환경에 따른 극소 float 오차는 허용한다.
* 계산식 변경으로 인한 의미 있는 수치 변화는 불일치로 본다.
* 결과를 강제로 덮어써서 맞추면 안 된다.

\---

## 17\. 성능 계산 공통식

성능 분석에서 사용하는 기본식은 다음과 같다.

```text
Speedup = Baseline Wall Time / Parallel Wall Time
Efficiency = Speedup / N × 100
```

여기서 `Baseline Wall Time`은 기본적으로 본인이 같은 환경에서 직접 실행한 Baseline 평균 Wall Time을 사용한다.
팀 전체 비교를 위해 361601 ms 같은 팀 기준값을 사용할 수 있지만, 이 경우에는 본인 측정값이 아니라 팀 기준값임을 보고서와 출력에 명시해야 한다.

여기서 `N`은 물리 CPU 전체 개수가 아니라, 해당 실험에서 동시에 계산에 투입된 active execution unit 수를 의미한다.

Amdahl’s Law 일반식:

```text
이론 Speedup = 1 / ((1 - P) + P / N)
```

주의:

* `common.md`에서는 일반식만 정의한다.
* 나/다/라 실험별 `N` 계산 방식과 확장 출력 방식은 `parallel.md`에서 정의한다.

\---

## 18\. P 값 해석 기준

P는 문맥에 따라 의미가 다르므로 반드시 구분한다.

### 18.1 Baseline 내부 참고 P

Baseline 코드 내부에서 다음처럼 계산되는 P는 “순차 코드의 작업 시간 중 병렬화 가능해 보이는 구간의 비율”을 대략 추정하는 참고값이다.

```c
par\_total = thread\_total\_ms + parent\_lighting\_ms
          + child1\_ai\_ms + child2\_particle\_ms
          + play\_roundrobin\_ms;
P = par\_total / total\_ms;
```

이 값은 Baseline 참고용이다.

### 18.2 병렬 실험 평가용 effective P

병렬 실험 결과 분석에서는 실측 Speedup과 active core 수를 이용해 effective P를 역산할 수 있다.

```text
P\_effective = (1 - 1/Speedup) / (1 - 1/N)
```

이 값은 실제 병렬화 효율, IPC, 동기화, context switching 등의 오버헤드가 반영된 유효 병렬화 비율이다.

따라서 Baseline 내부 참고 P와 병렬 실험 평가용 effective P를 혼동하면 안 된다.

\---

## 19\. Baseline 코드 주석 수정 권장 사항

`game_world_baseline_v4.c`의 실제 계산 기준은 현재 문서의 기준과 일치한다.

다만 코드 상단 주석이나 일부 오래된 설명에 다음과 같은 이전 버전 표현이 남아 있으면 수정한다.

```text
game_world_baseline_v2.c
path.txt
그리디 경로 계산
라운드 로빈 풀/물 움직임
```

현재 Baseline 코드 기준 설명은 다음과 같이 맞춘다.

```text
game_world_baseline_v4.c
게임 월드 초기화 시스템 — 단일 프로세스 순차 Baseline

[세팅 단계]
지형 생성 → 조도 계산 → 몬스터 AI → 파티클 시뮬레이션

[플레이 단계]
물리/AI → 명령서 생성 → GPU 렌더링

주의:
THREAD/PARENT/CHILD 라벨은 병렬화 시 역할 분담을 설명하기 위한 이름이다.
Baseline 코드 자체가 fork() 또는 pthread_create()를 사용하는 것은 아니다.

빌드:
gcc -O2 -o game_world_baseline_v4 game_world_baseline_v4.c -lm -lpthread
```

판정:

```text
계산 불일치 아님.
그러나 팀원 혼동 방지를 위해 코드 상단 주석 수정 권장.
```

\---

## 20\. 금지 사항

다음은 금지한다.

```text
Seed 변경 금지
Map 크기 변경 금지
Chunk 크기 변경 금지
반복 횟수 감소 금지
terrain\_blur()를 간이 blur로 대체 금지
침식 RNG 방식을 임의 변경 금지
무결성 검사 허용 오차 제거 금지
결과값 강제 보정 금지
출력값만 맞추고 계산을 생략하는 코드 금지
```

\---

## 21\. common.md와 parallel.md 역할 구분

```text
common.md
→ Baseline 계산 방식, 고정 상수, 검증값, 공통 성능 계산식

parallel.md
→ 나/다/라 병렬 구조, 실험별 active core 수 N, 확장 출력 항목, 코어별 사용률, Context Switching, Memory Usage
```

Baseline 코드가 parallel.md의 확장 출력 항목을 모두 출력하지 않아도 문제는 아니다.

그러나 나/다/라 병렬화 코드는 parallel.md의 확장 출력 기준을 따라야 한다.

