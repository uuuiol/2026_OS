# 게임 월드 병렬화 구현 기준서 — 병렬 전용 정리본

## 0\. 문서 목적

이 문서는 `common.md`의 Baseline 계산 방식을 그대로 유지한 상태에서, `나 / 다 / 라` 병렬화 실험을 어떻게 구성하고 어떤 결과를 출력해야 하는지 정의한다.

역할 구분:

```text
common.md
→ 계산 방식, 고정 상수, 검증값, Baseline 재현 기준

parallel.md
→ 병렬 구조, 실험별 출력 형식, active core 수 N, Speedup/Efficiency/Amdahl 분석, 코어별 사용률, Context Switching, IPC, Memory Usage
```

중요:

* 계산식, 상수, 반복 횟수, 검증값은 `common.md`를 따른다.
* 이 문서에는 병렬 구조와 계측/출력 기준만 둔다.
* Baseline 코드가 이 문서의 확장 출력 항목을 모두 출력하지 않아도 문제는 아니다.
* `나 / 다 / 라` 병렬화 코드는 이 문서의 확장 출력 항목을 최대한 구현해야 한다.
* 팀원 이름은 코드, 주석, 출력에 넣지 않는다.

\---

## 1\. 병렬화 공통 원칙

모든 병렬화 실험은 다음을 지킨다.

```text
1. common.md의 계산식과 반복 횟수를 변경하지 않는다.
2. 병렬화를 위해 계산량을 줄이지 않는다.
3. Baseline과 동일한 작업을 다른 실행 구조로 수행한다.
4. 병렬 구조만 바꾼다.
5. 결과 검증값은 Baseline과 같아야 한다.
6. 계산식 변경이 불가피한 경우 반드시 출력/보고서에 명시한다.
```

\---

## 2\. 병렬 코드 공통 출력 요구사항

`나 / 다 / 라` 모든 병렬화 코드는 다음 정보를 출력한다.

```text
System Core Count
CPU Pinning 여부
실험 구성
active core 수 N 및 계산 근거
세팅 단계 작업별 시간
플레이 단계 작업별 시간
프로세스/스레드 생성 오버헤드
세팅→플레이 전환 시간
Wall Time
Speedup
Efficiency
Amdahl's Law 검증
CPU user/sys 시간
CPU 활용률
세팅 단계 코어별 사용률
플레이 단계 코어별 사용률
Context Switching
IPC 오버헤드
Memory Usage
결과 검증값
```

IPC가 없는 구조에서는 IPC 항목을 삭제하지 말고 `N/A` 또는 `0`으로 표시한다.

\---

## 3\. active core 수 N 정의

Amdahl’s Law와 Efficiency 계산에서 사용하는 `N`은 물리 CPU 전체 개수가 아니다.

`N`은 **측정 구간에서 실제로 동시에 계산에 투입된 active execution unit 수**이다.

중요 기준:

```text
Parent가 조도 계산, 물리/AI, 명령서 생성 등 실제 계산을 동시에 수행하면 N에 포함한다.
Parent가 waitpid(), pipe read(), join(), barrier 대기만 하면 해당 구간 N에서 제외한다.
Child가 실제 계산을 수행하면 N에 포함한다.
Child가 내부 Thread 관리/대기만 하고 CPU 계산을 거의 하지 않으면 N\\\_compute에서는 제외할 수 있다.
Thread가 실제 chunk, render, physics 등의 계산을 수행하면 N에 포함한다.
```

따라서 출력에는 다음을 구분해서 적는다.

```text
실행 주체 수: 생성된 Parent / Child / Thread 개수
active core 수 N: 해당 측정 구간에서 실제 계산에 투입된 수
N 계산 근거: Parent가 계산 중인지, 대기 중인지 명시
```

예시:

```text
Parent + Thread 4개, Parent가 조도 계산 동시 수행 → N = 5
Parent + Child 4개, Parent가 조도 계산 동시 수행 → N = 5
Parent + Child 4개, Parent가 waitpid로 대기 → N = 4
Parent + Thread 4개, Parent가 join으로 대기 → N = 4
Parent + Child 2 + Thread 4, 모두 계산 참여 → N = 7
```

주의:

* `N=7`은 전체 공통값이 아니다.
* `N`은 실험 구성과 측정 구간마다 달라진다.
* 세팅 단계와 플레이 단계의 N이 다르면 각각 `N\\\_setting`, `N\\\_play`로 분리 출력한다.
* 총 Speedup/Efficiency 계산에 사용할 N은 보고서에 근거를 반드시 적는다.

\---

## 4\. Speedup / Efficiency / Amdahl 계산 기준

### 4.1 Speedup

```text
Speedup = Baseline Wall Time / Parallel Wall Time
```

Baseline Wall Time 적용 원칙:

```text
Baseline Wall Time = 본인이 같은 환경에서 직접 실행한 Baseline 평균 Wall Time
```

팀 공통 비교가 필요한 경우에만 아래 값을 기준값으로 사용할 수 있다.
단, 이 경우에도 보고서/출력에 "팀 기준값 사용"이라고 명시한다.

```text
팀 기준 Baseline Wall Time = 361601 ms
```

예시:

```text
본인 Baseline 평균 Wall Time = 310833 ms
Parallel Wall Time = 99912 ms
Speedup = 310833 / 99912 ≈ 3.11x
```

주의:

* Speedup 계산의 기본 기준은 본인이 측정한 Baseline Wall Time이다.
* Baseline은 병렬 코드와 같은 환경, 같은 컴파일 기준, 같은 입력 조건에서 측정한다.
* 3회 반복 실행한 경우 Baseline 평균 Wall Time을 사용한다.
* 팀 기준값 361601 ms를 사용할 경우, 본인 측정값이 아니라 팀 기준값임을 명시한다.
* 같은 실험표 안에서는 하나의 Baseline Wall Time 기준값을 통일한다.

### 4.2 Efficiency

```text
Efficiency = Speedup / N × 100
```

여기서 `N`은 해당 실험의 active execution unit 수이다.

### 4.3 Amdahl’s Law 이론값

```text
S\\\_theory = 1 / ((1 - P) + P / N)
```

### 4.4 effective P 역산

병렬 실험 결과에서 유효 병렬화 비율은 다음 식으로 역산한다.

```text
P\\\_effective = (1 - 1/Speedup) / (1 - 1/N)
```

주의:

* Baseline 내부 참고용 P와 병렬 실험 평가용 `P\\\_effective`는 다르다.
* 병렬 실험 보고서에서는 `Speedup`과 `N`으로 역산한 `P\\\_effective`를 별도 표기한다.

\---

# 5\. 나 실험 기준 — 단일 Child vs 단일 Thread 비교

## 5.1 실험 목적

단일 worker를 사용할 때 `fork()` 기반 Child와 `pthread\\\_create()` 기반 Thread의 성능 차이를 비교한다.

비교 대상:

```text
1. Parent + 단일 Child
2. Parent + 단일 Thread
```

핵심 비교 항목:

```text
fork() 생성 비용
pthread\\\_create() 생성 비용
IPC 비용
공유 메모리/전역 메모리 접근 차이
Context Switching 차이
Baseline 대비 Speedup
```

## 5.2 나 실험 구조

### Parent + 단일 Child

```text
Parent:
  초기화
  조도/기후 계산
  Child 결과 수신
  AI / 파티클 / 플레이 단계 진행 또는 측정

Child:
  지형 생성 전체 담당
  noise → merge → blur → erosion → histogram/integrity 수행
  완료 후 Parent에게 결과 전달
```

주의:

* 이 실험의 worker 담당 범위는 `noise만`이 아니다.
* worker는 지형 생성 전체, 즉 `noise → merge → blur → erosion` 흐름을 담당한다.
* `terrain\\\_blur()`는 common.md 기준을 유지한다.
* Child 구조에서는 결과 전달 IPC 비용을 출력한다.

### Parent + 단일 Thread

```text
Parent:
  초기화
  조도/기후 계산
  Thread 완료 확인
  AI / 파티클 / 플레이 단계 진행 또는 측정

Thread:
  지형 생성 전체 담당
  noise → merge → blur → erosion → histogram/integrity 수행
```

주의:

* Thread 구조에서도 worker 담당 범위는 지형 생성 전체이다.
* Child 버전과 Thread 버전은 동일 작업 범위를 비교해야 한다.
* 차이는 `fork()` 기반 독립 주소 공간인지, `pthread\\\_create()` 기반 공유 주소 공간인지이다.

\---

## 5.3 나 실험 출력 형식

```text
===========================================================
=== 게임 월드 병렬 실험 — 나 ===
시드: 0xDEAD4096 (고정) | 맵: 1024×1024 | 청크: 256개

\\\[CORE] 시스템 코어 수: XX개
\\\[PIN] PARENT → cpu0 / 미적용

===========================================================
\\\[실험 구성]
===========================================================
  구성: Parent + 단일 Child 또는 Parent + 단일 Thread
  active core 수 N: X
  비교 목적: fork() 비용 vs pthread\\\_create() 비용

===========================================================
\\\[세팅 단계]
===========================================================
  \\\[WORKER] 지형 생성 전체:  XXXXX.XX ms  (noise→merge→blur→erosion)
  \\\[PARENT] 조도 계산:       XXXXX.XX ms  (WORKER와 동시 실행 가능)
  병합:                     XXXXX.XX ms
  블러:                     XXXXX.XX ms  <- common.md 기준
  침식:                     XXXXX.XX ms
  히스토그램/무결성:         XXXXX.XX ms
  \\\[CHILD1] 몬스터 AI:       XXXXX.XX ms  (순차 실행 시에도 출력)
  \\\[CHILD2] 파티클 4종:      XXXXX.XX ms  (순차 실행 시에도 출력)
  세팅 단계 Wall Time:      XXXXX.XX ms
  세팅 순차 환산:           XXXXX.XX ms
  세팅 Speedup:                 X.XXx
  세팅 Efficiency:              XX.X%  (= Speedup / N)

\\\[프로세스/스레드 생성]
  fork() × 1:               X.XXX ms  또는 N/A
  pthread\\\_create() × 1:     X.XXX ms  또는 N/A
  생성 총 오버헤드:          X.XXX ms

===========================================================
\\\[플레이 단계] 500틱
===========================================================
  \\\[PARENT] 물리/AI 연산:    XXXXX.XX ms
  \\\[PARENT] 명령서 생성:     XXXXX.XX ms
  \\\[WORKER] GPU 렌더링:      XXXXX.XX ms
  플레이 Wall Time:         XXXXX.XX ms
  플레이 순차 환산:         XXXXX.XX ms
  플레이 Speedup:               X.XXx
  플레이 Efficiency:            XX.X%  (= Speedup / N)

===========================================================
  총 Wall Time:             XXXXX.XX ms
  총 Speedup:                   X.XXx  (기준: Baseline XXXXX.XX ms)
  총 Efficiency:                XX.X%  (= Speedup / N)
===========================================================
\\\[Amdahl's Law 검증]
  active core 수 N:          X
  병렬화 비율 P\\\_effective:   XX.X%
  이론 Speedup:              X.XXx
  실측 Speedup:              X.XXx
  차이:                      X.XXx
===========================================================
\\\[CPU 성능 지표]
  CPU user 시간:             XXXXX.XX ms
  CPU sys  시간:             XXXXX.XX ms
  CPU 활용률:                   XXX.X%
===========================================================
\\\[세팅 단계 코어별]        \\\[플레이 단계 코어별]
  cpu0 (PARENT):   XX.X%    cpu0 (PARENT):   XX.X%
  cpu1 (WORKER1):  XX.X%    cpu1 (RENDER1):  XX.X%
  cpu2 (WORKER2):  XX.X%    cpu2 (RENDER2):  XX.X%
  ...
  cpuK (WORKERK):  XX.X%    cpuK (RENDERK):  XX.X%
  나머지:          X.X%     나머지:          X.X%
  ※ 나 실험에서는 K=1로 해석한다. 즉 cpu1은 세팅 단계 WORKER1, 플레이 단계 RENDER1이다.
===========================================================
\\\[컨텍스트 스위칭]
               voluntary    nonvoluntary
  세팅 단계:    XXXX 회      XXXX 회
  플레이 단계:  XXXX 회      XXXX 회
  Baseline 대비:+XXXX 회     +XXXX 회 증가
===========================================================
\\\[IPC 오버헤드]
  pipe/shared memory/none:  XXXXX
  총 오버헤드:              X.XX ms 또는 N/A
===========================================================
\\\[메모리 사용량]
  세팅 완료 후 RSS:          XXXXX KB
  세팅 완료 후 Peak:         XXXXX KB
  플레이 완료 후 RSS:        XXXXX KB
  플레이 완료 후 Peak:       XXXXX KB
===========================================================
\\\[결과 검증 — Baseline과 동일해야 함]
  평균 조도:                 0.397698  ✓
  AI 최고점수:               97.7063   ✓
  파티클 에너지:             25324.066406  ✓
  무결성 실패:               0         ✓
===========================================================
```

\---

# 6\. 다 실험 기준 — Child 수 / Thread 수 확장성 비교

## 6.1 실험 목적

worker 수를 `1 → 2 → 4 → 8`로 늘릴 때 성능이 어떻게 변하는지 비교한다.

비교 대상:

```text
Child 수:  1 → 2 → 4 → 8
Thread 수: 1 → 2 → 4 → 8
```

핵심 관찰:

```text
Speedup 증가 여부
Efficiency 감소 여부
8개 이상에서 성능 저하 발생 여부
Context Switching 증가 여부
IPC 오버헤드 증가 여부
```

## 6.2 다 실험 active core 수 및 Barrier 구조

### active core 수 N

다 실험에서는 Child 구조와 Thread 구조의 `N` 계산을 분리한다.

```text
Thread 구조:
  Parent가 조도/기후 계산을 worker와 동시에 수행하면 N = Thread 수 K + Parent 1
  Parent가 join 대기만 하면 N = Thread 수 K

Child 구조:
  Parent가 조도/기후 계산을 Child와 동시에 수행하면 N = Child 수 K + Parent 1
  Parent가 waitpid/read 대기만 하면 N = Child 수 K
```

예시:

```text
Thread 4개 + Parent 조도 동시 실행 → N = 5
Thread 4개 + Parent join 대기 → N = 4
Child 4개 + Parent 조도 동시 실행 → N = 5
Child 4개 + Parent waitpid 대기 → N = 4
```

출력에는 반드시 다음을 적는다.

```text
Worker 종류: Child 또는 Thread
Worker 수 K: X
Parent 동시 계산 여부: 계산 / 대기
active core 수 N: X
N 계산 근거: ...
```

### Barrier 구조

다 실험에서 worker가 지형 생성을 분할 수행하는 경우에도 Barrier 번호는 `game\\\_world\\\_baseline\\\_v4.c`의 실제 출력 기준에 맞춘다.

Baseline 코드의 지형 생성 순서는 다음과 같다.

```text
noise\\\_compute\\\_chunk()
↓
terrain\\\_merge()
↓  BARRIER POINT 1: 병합 완료 후 blur 시작 가능
terrain\\\_blur()
↓  BARRIER POINT 2: blur 완료 후 erosion 시작 가능
terrain\\\_erosion()
↓  BARRIER POINT 3: erosion 완료 후 histogram/integrity 시작 가능
terrain\\\_histogram() + terrain\\\_integrity()
```

따라서 병렬 코드의 Barrier 의미는 다음 3개로 통일한다.

```text
BARRIER POINT 1
→ terrain\\\_merge() 완료 후 terrain\\\_blur() 시작 가능

BARRIER POINT 2
→ terrain\\\_blur() 완료 후 terrain\\\_erosion() 시작 가능

BARRIER POINT 3
→ terrain\\\_erosion() 완료 후 terrain\\\_histogram()/terrain\\\_integrity() 시작 가능
```

주의:

* noise 완료 후 merge 시작 가능 여부는 worker 종료, join, waitpid, pipe read, shared memory flag 등으로 반드시 보장한다.
* 다만 Baseline 코드의 출력 기준에서는 noise 완료 지점을 별도 BARRIER 번호로 세지 않는다.
* `terrain\\\_blur()` 단계에서 worker별 snapshot이 섞이면 안 된다.
* barrier 대기 시간은 가능하면 최소/최대/평균으로 출력한다.
* Child 구조에서 barrier 구현이 어렵거나 다른 동기화 방식을 쓰면 `pipe`, `waitpid`, `shared memory flag` 등 실제 동기화 방식을 명시한다.

\---

## 6.3 다 실험 출력 형식

각 worker 수마다 동일한 출력 블록을 반복 출력한다.

```text
===========================================================
=== 게임 월드 병렬 실험 — 다 ===
시드: 0xDEAD4096 (고정) | 맵: 1024×1024 | 청크: 256개

\\\[CORE] 시스템 코어 수: XX개
\\\[PIN] PARENT → cpu0 / 미적용

===========================================================
\\\[실험 구성]
===========================================================
  Worker 종류: Child 또는 Thread
  Worker 수 K: X
  Parent 동시 계산 여부: 계산 / 대기
  active core 수 N: X
  N 계산 근거: Parent 포함 / Parent 제외

===========================================================
\\\[세팅 단계]
===========================================================
  \\\[WORKER×K] 노이즈 연산:   XXXXX.XX ms  (완료 후 merge 가능해야 함)
  병합:                     XXXXX.XX ms  <- 완료 후 BARRIER POINT 1
  블러:                     XXXXX.XX ms  <- BARRIER POINT 1 이후, 완료 후 BARRIER POINT 2, common.md 기준
  침식:                     XXXXX.XX ms  <- BARRIER POINT 2 이후, 완료 후 BARRIER POINT 3
  히스토그램/무결성:         XXXXX.XX ms  <- BARRIER POINT 3 이후
  \\\[PARENT] 조도 계산:       XXXXX.XX ms  (가능한 경우 worker와 동시 실행)
  세팅 단계 Wall Time:      XXXXX.XX ms
  세팅 순차 환산:           XXXXX.XX ms
  세팅 Speedup:                 X.XXx
  세팅 Efficiency:              XX.X%  (= Speedup / N)

\\\[프로세스/스레드 생성]
  fork() × K:               X.XXX ms 또는 N/A
  pthread\\\_create() × K:     X.XXX ms 또는 N/A
  생성 총 오버헤드:          X.XXX ms

===========================================================
\\\[플레이 단계] 500틱
===========================================================
  \\\[PARENT] 물리/AI 연산:    XXXXX.XX ms
  \\\[PARENT] 명령서 생성:     XXXXX.XX ms
  \\\[WORKER×K] GPU 렌더링:    XXXXX.XX ms
  플레이 Wall Time:         XXXXX.XX ms
  플레이 순차 환산:         XXXXX.XX ms
  플레이 Speedup:               X.XXx
  플레이 Efficiency:            XX.X%  (= Speedup / N)

\\\[동기화/Barrier 대기 시간]
  선행 동기화(noise 완료→merge 시작): min X.XX ms | max X.XX ms | avg X.XX ms
  BARRIER 1(merge 완료→blur 시작):    min X.XX ms | max X.XX ms | avg X.XX ms
  BARRIER 2(blur 완료→erosion 시작):  min X.XX ms | max X.XX ms | avg X.XX ms
  BARRIER 3(erosion 완료→hist/integrity): min X.XX ms | max X.XX ms | avg X.XX ms

===========================================================
  총 Wall Time:             XXXXX.XX ms
  총 Speedup:                   X.XXx  (기준: Baseline XXXXX.XX ms)
  총 Efficiency:                XX.X%  (= Speedup / N)
===========================================================
\\\[Amdahl's Law 검증]
  Worker 수 K:              X
  active core 수 N:          X
  병렬화 비율 P\\\_effective:   XX.X%
  이론 Speedup:              X.XXx
  실측 Speedup:              X.XXx
  차이:                      X.XXx
===========================================================
\\\[CPU 성능 지표]
  CPU user 시간:             XXXXX.XX ms
  CPU sys  시간:             XXXXX.XX ms
  CPU 활용률:                   XXX.X%
===========================================================
\\\[세팅 단계 코어별]        \\\[플레이 단계 코어별]
  cpu0 (PARENT):   XX.X%    cpu0 (PARENT):   XX.X%
  cpu1 (WORKER1):  XX.X%    cpu1 (RENDER1):  XX.X%
  cpu2 (WORKER2):  XX.X%    cpu2 (RENDER2):  XX.X%
  ...
  cpuK (WORKERK):  XX.X%    cpuK (RENDERK):  XX.X%
  나머지:          X.X%     나머지:          X.X%
===========================================================
\\\[컨텍스트 스위칭]
               voluntary    nonvoluntary
  세팅 단계:    XXXX 회      XXXX 회
  플레이 단계:  XXXX 회      XXXX 회
  Baseline 대비:+XXXX 회     +XXXX 회 증가
===========================================================
\\\[IPC 오버헤드]
  pipe/shared memory/none:  XXXXX
  총 오버헤드:              X.XX ms 또는 N/A
===========================================================
\\\[메모리 사용량]
  세팅 완료 후 RSS:          XXXXX KB
  세팅 완료 후 Peak:         XXXXX KB
  플레이 완료 후 RSS:        XXXXX KB
  플레이 완료 후 Peak:       XXXXX KB
===========================================================
\\\[결과 검증 — Baseline과 동일해야 함]
  평균 조도:                 0.397698  ✓
  AI 최고점수:               97.7063   ✓
  파티클 에너지:             25324.066406  ✓
  무결성 실패:               0         ✓
===========================================================
```

\---

# 7\. 라 실험 기준 — Child × Thread 혼합 최적화

## 7.1 실험 목적

Child와 Thread를 혼합하여 어떤 조합이 가장 효율적인지 비교한다.

예시 조합:

```text
Child 4 × Thread 2
Child 2 × Thread 4
Child 8 × Thread 1
```

핵심 관찰:

```text
Child와 Thread 역할 분담의 효율
IPC 비용과 공유 메모리 비용 비교
Thread 내부 동기화 비용
Child 증가에 따른 context switching 비용
혼합 구조에서 최적 조합 탐색
```

## 7.2 라 실험 역할 정의

### Parent

```text
초기화
기후/조도 연산
Child 작업 큐 관리
Chunk 배정 스케줄링
Child 결과 수집
height map 병합
최종 결과 검증
```

### Child

```text
Parent에게 chunk 작업을 배정받음
내부 Thread에게 noise 연산 분배
Thread들이 완성한 chunk 결과 수집
경계면 Blur 처리
평활화
침식
무결성 검사
완성된 결과를 Parent에게 전달
```

### Thread

```text
실제 chunk 단위 계산 담당
Perlin Noise 계산
Monte Carlo 고도값 계산
chunk\\\_blur 수행
```

주의:

* Thread는 실제 chunk 계산의 최소 작업 단위이다.
* Child는 내부 Thread를 관리하는 중간 실행 주체이다.
* Parent는 전체 오케스트레이션과 결과 수집을 담당한다.

## 7.3 라 실험 active core 수

라 실험에서는 `생성된 실행 주체 수`와 `실제 계산에 투입된 active core 수 N`을 구분한다.

기본 원칙:

```text
N = 해당 측정 구간에서 실제 CPU 계산을 수행한 Parent / Child / Thread 수
```

역할별 판단 기준:

```text
Parent:
  조도/기후, 물리/AI, 명령서 생성 등 계산 수행 중이면 N에 포함
  waitpid, pipe read, join, barrier 대기만 하면 N에서 제외

Child:
  blur, erosion, histogram, integrity, scheduling 계산을 실제 수행하면 N에 포함
  내부 Thread 생성 후 관리/대기만 하면 N\\\_compute에서는 제외 가능

Thread:
  chunk noise, render, physics 등 실제 계산을 수행하면 N에 포함
```

예시:

```text
Child 2개 + 총 Thread 4개 + Parent 계산 참여
→ 생성 주체: Parent 1 + Child 2 + Thread 4
→ N\\\_compute = 7

Child 4개, 각 Child 내부 Thread 2개, Child는 관리/대기만 수행
→ 생성 주체: Parent 1 + Child 4 + Thread 8
→ N\\\_compute = Parent가 계산하면 9, Parent가 대기하면 8

Child 4개, 각 Child 내부 Thread 2개, Child도 blur/erosion 계산 수행
→ 생성 주체: Parent 1 + Child 4 + Thread 8
→ N\\\_compute = Parent 포함 시 13, Parent 대기 시 12
```

출력에는 반드시 다음을 함께 표시한다.

```text
생성 주체 수: Parent X, Child X, Thread X
active core 수 N: X
N 계산 근거: Parent/Child/Thread 중 누가 실제 계산 중인지
```

\---

## 7.4 라 실험 출력 형식

```text
===========================================================
=== 게임 월드 병렬 실험 — 라 ===
시드: 0xDEAD4096 (고정) | 맵: 1024×1024 | 청크: 256개

\\\[CORE] 시스템 코어 수: XX개
\\\[PIN] PARENT → cpu0 / 미적용

===========================================================
\\\[실험 구성]
===========================================================
  구성: Child X × Thread Y
  Child 수: X
  Thread 수: Y
  생성 주체 수: Parent 1 + Child X + Thread Y
  active core 수 N: Z
  N 계산 근거: Parent/Child/Thread 실제 계산 참여 기준

===========================================================
\\\[세팅 단계]
===========================================================
----- \\\[CHILD×X / THREAD×Y] 지형 맵 생성 -----
  노이즈:                   XXXXX.XX ms  (THREAD 병렬, 완료 후 merge 가능해야 함)
  병합:                     XXXXX.XX ms  <- 완료 후 BARRIER POINT 1
  블러:                     XXXXX.XX ms  <- BARRIER POINT 1 이후, 완료 후 BARRIER POINT 2, common.md 기준
  침식:                     XXXXX.XX ms  <- BARRIER POINT 2 이후, 완료 후 BARRIER POINT 3
  히스토그램:               XXXXX.XX ms  <- BARRIER POINT 3 이후
  소계:                     XXXXX.XX ms
-----------------------------------------------------------
  \\\[PARENT] 조도 계산:       XXXXX.XX ms  (CHILD/THREAD와 동시 실행)
  \\\[CHILD] 관리/수집 비용:   XXXXX.XX ms
  세팅 단계 Wall Time:      XXXXX.XX ms  (= max 기준)
  세팅 순차 환산:           XXXXX.XX ms
  세팅 Speedup:                 X.XXx
  세팅 Efficiency:              XX.X%  (= Speedup / N)

\\\[프로세스/스레드 생성]
  fork() × X:               X.XXX ms
  pthread\\\_create() × Y:     X.XXX ms
  생성 총 오버헤드:          X.XXX ms

===========================================================
\\\[세팅→플레이 전환]
===========================================================
  Child waitpid():          X.XXX ms
  추가 pthread\\\_create():    X.XXX ms 또는 N/A
  전환 총 시간:              X.XXX ms

===========================================================
\\\[플레이 단계] 500틱
===========================================================
  \\\[PARENT] 물리/AI 연산:    XXXXX.XX ms
  \\\[PARENT] 명령서 생성:     XXXXX.XX ms
  \\\[THREAD] GPU 렌더링:      XXXXX.XX ms
  플레이 Wall Time:         XXXXX.XX ms
  플레이 순차 환산:         XXXXX.XX ms
  플레이 Speedup:               X.XXx
  플레이 Efficiency:            XX.X%  (= Speedup / N)

\\\[동기화/Barrier 대기 시간]
  선행 동기화(noise 완료→merge 시작): 최소 X.XX ms | 최대 X.XX ms | 평균 X.XX ms
  BARRIER 1(merge 완료→blur 시작):    최소 X.XX ms | 최대 X.XX ms | 평균 X.XX ms
  BARRIER 2(blur 완료→erosion 시작):  최소 X.XX ms | 최대 X.XX ms | 평균 X.XX ms
  BARRIER 3(erosion 완료→hist/integrity): 최소 X.XX ms | 최대 X.XX ms | 평균 X.XX ms

===========================================================
  총 Wall Time:             XXXXX.XX ms
  총 Speedup:                   X.XXx  (기준: Baseline XXXXX.XX ms)
  총 Efficiency:                XX.X%  (= Speedup / N)
===========================================================
\\\[태스크별 비율]
  THREAD(지형):  XX.X%   PARENT(조도): XX.X%
  CHILD 관리:    XX.X%   PLAY:         XX.X%
===========================================================
\\\[Amdahl's Law 검증]
  active core 수 N:          Z
  병렬화 비율 P\\\_effective:   XX.X%
  이론 Speedup:              X.XXx
  실측 Speedup:              X.XXx
  차이:                      X.XXx
===========================================================
\\\[CPU 성능 지표]
  CPU user 시간:             XXXXX.XX ms
  CPU sys  시간:             XXXXX.XX ms
  CPU 활용률:                   XXX.X%
===========================================================
\\\[세팅 단계 코어별]        \\\[플레이 단계 코어별]
  cpu0 (PARENT):   XX.X%    cpu0 (PARENT):   XX.X%
  cpu1 (WORKER1):  XX.X%    cpu1 (RENDER1):  XX.X%
  cpu2 (WORKER2):  XX.X%    cpu2 (RENDER2):  XX.X%
  ...
  cpuK (WORKERK):  XX.X%    cpuK (RENDERK):  XX.X%
  나머지:          X.X%     나머지:          X.X%
  ※ 라 실험에서도 출력 형식은 동일하게 맞춘다. 세팅 단계의 WORKER는 해당 코어에서 실제 계산한 Child 또는 Thread를 포괄하는 이름이다.
===========================================================
\\\[컨텍스트 스위칭]
               voluntary    nonvoluntary
  세팅 단계:    XXXX 회      XXXX 회
  플레이 단계:  XXXX 회      XXXX 회
  Baseline 대비:+XXXX 회     +XXXX 회 증가
===========================================================
\\\[IPC 오버헤드]
  \\\[세팅 단계 — pipe]
    CHILD→PARENT write: X.XX ms | XXXXX bytes
    CHILD→PARENT read:  X.XX ms
    pipe 총 오버헤드:   X.XX ms
  \\\[세팅 단계 — 공유 메모리]
    shm write:          X.XX ms | XXXXX bytes
    shm read:           X.XX ms
    shm 총 오버헤드:    X.XX ms
===========================================================
\\\[메모리 사용량]
  세팅 완료 후 RSS:          XXXXX KB
  세팅 완료 후 Peak:         XXXXX KB
  플레이 완료 후 RSS:        XXXXX KB
  플레이 완료 후 Peak:       XXXXX KB
===========================================================
\\\[결과 검증 — Baseline과 동일해야 함]
  평균 조도:                 0.397698  ✓
  AI 최고점수:               97.7063   ✓
  파티클 에너지:             25324.066406  ✓
  무결성 실패:               0         ✓
===========================================================
```

\---

# 8\. 계측 기준

## 8.1 코어별 사용률

코어별 사용률은 `/proc/stat`을 이용해 측정한다.

측정 방식:

```text
1. 세팅 단계 시작 전 /proc/stat snapshot 저장
2. 세팅 단계 종료 후 /proc/stat snapshot 저장
3. delta\\\_total, delta\\\_idle 계산
4. usage = (delta\\\_total - delta\\\_idle) / delta\\\_total × 100
```

플레이 단계도 동일하게 별도 snapshot을 사용한다.

코어별 사용률 출력은 `나 / 다 / 라` 모두 다음 형식으로 통일한다.

```text
\[세팅 단계 코어별]        \[플레이 단계 코어별]
  cpu0 (PARENT):   XX.X%    cpu0 (PARENT):   XX.X%
  cpu1 (WORKER1):  XX.X%    cpu1 (RENDER1):  XX.X%
  cpu2 (WORKER2):  XX.X%    cpu2 (RENDER2):  XX.X%
  ...
  cpuK (WORKERK):  XX.X%    cpuK (RENDERK):  XX.X%
  나머지:          X.X%     나머지:          X.X%
```

주의:

```text
1. `cpu1\~cpuK (WORKER 평균)`처럼 평균값 한 줄로만 출력하지 않는다.
2. 각 코어별 사용률을 개별 라인으로 출력한다.
3. 나 실험은 K=1로 해석한다.
4. 다 실험은 K=Thread 수로 해석한다.
5. 라 실험은 해당 측정 구간에서 실제 계산에 참여한 Worker/Render 실행 주체 수를 K로 해석한다.
6. CPU Pinning이 적용되지 않은 경우 역할명은 참고 표기이며, 실제 OS 스케줄러가 배정한 코어 사용률임을 출력/보고서에 명시한다.
```

## 8.2 CPU Pinning

가능하면 역할별 CPU affinity를 고정한다.

예시:

```text
cpu0 → Parent
cpu1\\\~cpu4 → Thread
cpu5\\\~cpu6 → Child
cpu7 → OS 여유
cpu8\\\~ → 미사용 또는 OS/background
```

Pinning이 불가능하면 다음처럼 출력한다.

```text
\\\[PIN] 미적용: 권한 부족 / 플랫폼 미지원 / 설정 생략
```

## 8.3 Context Switching

가능하면 `getrusage()`를 이용해 voluntary / nonvoluntary context switch 수를 출력한다.

```c
ru.ru\\\_nvcsw   // voluntary context switches
ru.ru\\\_nivcsw  // nonvoluntary context switches
```

Child 구조에서는 Parent와 Child 결과를 합산하거나, 측정 한계를 명시한다.

## 8.4 Memory Usage

가능하면 다음을 출력한다.

```text
RSS
Peak RSS
```

Linux에서는 다음 정보를 사용할 수 있다.

```text
/proc/self/status 의 VmRSS, VmHWM
getrusage()의 ru\\\_maxrss
```

Child 구조에서는 Parent 기준 메모리와 Child 종료 후 회수되는 메모리를 구분해 설명한다.

## 8.5 IPC 오버헤드

Child 구조에서는 Parent와 Child 간 결과 전달 비용을 출력한다.

측정 대상:

```text
pipe write 시간
pipe read 시간
전송 bytes
shared memory write/read 시간
```

Thread 구조에서는 IPC가 없으므로 다음처럼 표시한다.

```text
IPC 오버헤드: N/A (Thread 구조, 주소 공간 공유)
```

## 8.6 Barrier 기준

Barrier는 계산 순서 보장 지점이다.

Barrier 번호는 `game\\\_world\\\_baseline\\\_v4.c`의 실제 출력 기준에 맞춘다.

```text
BARRIER POINT 1
→ terrain\\\_merge() 완료 후 terrain\\\_blur() 시작 가능

BARRIER POINT 2
→ terrain\\\_blur() 완료 후 terrain\\\_erosion() 시작 가능

BARRIER POINT 3
→ terrain\\\_erosion() 완료 후 terrain\\\_histogram()/terrain\\\_integrity() 시작 가능
```

중요:

```text
noise\\\_compute\\\_chunk() 완료 후 terrain\\\_merge()가 시작되어야 한다.
단, 이 지점은 Baseline 코드 출력 기준에서 별도 BARRIER 번호로 세지 않는다.
병렬 코드에서는 join, waitpid, pipe read, shared memory flag 등 실제 동기화 방식으로 보장한다.
```

금지:

```text
한 worker가 blur 결과를 쓰는 중 다른 worker가 그 값을 읽어 snapshot을 만드는 구조
terrain\\\_blur()를 계산량이 적은 임의 row blur로 대체하는 구조
```

\---

# 9\. 실험 반복 실행 및 기록 표 기준

성능 측정은 가능하면 동일 조건에서 3회 반복 실행하고 평균값을 사용한다.

```text
1회차 / 2회차 / 3회차 / 평균
```

반복 실행 기준:

```text
같은 실행 파일 사용
같은 컴파일 옵션 사용
같은 Baseline Wall Time 기준 사용
같은 입력 Seed 사용
같은 Map/Chunk/Tick/Sample 수 사용
실행 중 다른 무거운 작업은 가능한 한 종료
```

Baseline Wall Time 기준값:

```text
Baseline Wall Time = 본인이 같은 환경에서 직접 실행한 Baseline 평균 Wall Time
```

팀 전체 비교 목적으로 361601 ms를 사용할 경우에는 다음처럼 명시한다.

```text
Baseline Wall Time = 361601 ms (팀 기준값 사용)
```

실험 기록 표 양식:

주의:

```text
N은 표에 고정값으로 미리 박지 않는다.
각 실행에서 Parent/Child/Thread 중 실제 계산에 참여한 주체를 기준으로 기록한다.
Parent가 조도/물리/명령서 생성 등 계산을 동시에 수행하면 N에 포함한다.
Parent가 waitpid(), join(), pipe read(), barrier 대기만 하면 N에서 제외한다.
```

|실험|구성|N|1회차 Wall ms|2회차 Wall ms|3회차 Wall ms|평균 Wall ms|Speedup|Efficiency|무결성 실패|비고|
|-|-|-|-:|-:|-:|-:|-:|-:|-:|-|
|나|Parent + Child 1|실제 구조 기준|||||||||
|나|Parent + Thread 1|실제 구조 기준|||||||||
|다|Child 1|실제 구조 기준|||||||||
|다|Child 2|실제 구조 기준|||||||||
|다|Child 4|실제 구조 기준|||||||||
|다|Child 8|실제 구조 기준|||||||||
|다|Thread 1|실제 구조 기준|||||||||
|다|Thread 2|실제 구조 기준|||||||||
|다|Thread 4|실제 구조 기준|||||||||
|다|Thread 8|실제 구조 기준|||||||||
|라|Child 4 × Thread 2|실제 구조 기준|||||||||
|라|Child 2 × Thread 4|실제 구조 기준|||||||||
|라|Child 8 × Thread 1|실제 구조 기준|||||||||

순차 환산 시간 정의:

```text
순차 환산 시간 = 병렬로 동시에 수행한 동일 작업들을 Baseline처럼 순차로 수행했을 때의 시간 합계
```

예:

```text
세팅 순차 환산 = 지형 생성 시간 + 조도 계산 시간 + AI 시간 + 파티클 시간
세팅 Speedup = 세팅 순차 환산 / 세팅 Wall Time
플레이 순차 환산 = 물리/AI 시간 + 명령서 생성 시간 + GPU 렌더 시간
플레이 Speedup = 플레이 순차 환산 / 플레이 Wall Time
```

\---

# 10\. 코드 / 문서 / 주석 우선순위

코드와 문서 또는 주석이 서로 다를 때는 다음 우선순위를 따른다.

```text
1순위: 실제 실행되는 구현 코드
2순위: common.md의 계산 기준
3순위: 이 parallel.md의 병렬 출력/분석 기준
4순위: 코드 상단 주석 또는 오래된 설명 주석
```

계산식은 common.md를 우선한다.

출력 형식과 병렬 실험 분석 항목은 이 parallel.md를 우선한다.

\---

# 11\. 병렬 실험 최종 체크리스트

```text
\\\[ ] 실험명 출력: 나 / 다 / 라
\\\[ ] 팀원 이름 미출력
\\\[ ] System Core Count 출력
\\\[ ] CPU Pinning 여부 출력
\\\[ ] active core 수 N 계산 근거 출력
\\\[ ] fork() / pthread\\\_create() 생성 오버헤드 출력
\\\[ ] 세팅→플레이 전환 시간 출력
\\\[ ] 순차 환산 시간 출력
\\\[ ] Speedup 출력
\\\[ ] Efficiency 출력
\\\[ ] Amdahl's Law 검증 출력
\\\[ ] Baseline P와 effective P 구분
\\\[ ] CPU user/sys 출력
\\\[ ] 코어별 사용률 출력
\\\[ ] Context Switching 출력
\\\[ ] IPC 오버헤드 출력 또는 N/A 표시
\\\[ ] Memory Usage 출력
\\\[ ] 결과 검증값 출력
\\\[ ] 무결성 실패 0 확인
\\\[ ] 가능하면 3회 반복 실행
\\\[ ] 1회차/2회차/3회차/평균 표 작성
\\\[ ] Baseline Wall Time 기준 명시: 본인 측정 Baseline 평균 Wall Time 또는 팀 기준값 361601 ms 사용 여부
\\\[ ] 8개 이상 worker에서 성능 저하 여부 관찰
```

